_site/
test-results/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
vendor/
.bundle/
.playwright-mcp/
*.tmp

# Local, ephemeral retrospective queue (the SessionEnd hook appends here). The
# durable record of what shipped is _data/retrospectives.yml. See docs/RETROSPECTIVE-HOOK.md.
.claude/retrospectives/

# Gemfile.lock is intentionally NOT ignored — committing it pins github-pages and
# html-proofer so CI is reproducible. Generate it once with `bundle install`.

# Local API credentials for scripts/generate-preview-images.sh (see .env.example).
.env

# Throwaway per-section scan dirs from scripts/generate-preview-images.sh --section
.preview-section.*

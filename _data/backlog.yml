# =============================================================================
# Content backlog — the autopilot's to-do list
# -----------------------------------------------------------------------------
# .claude/skills/grow-lifehacker reads this, picks the highest-priority item
# whose `status: todo`, produces it (with the right voice profile + screenshots),
# flips it to `done`, and opens a PR. Humans can add ideas here any time.
#
# item: { id, kind: hack|tool|post|doc, title, brief, voice, priority, status }
# priority: P1 (next) .. P3 (someday).  status: todo | drafting | done
# =============================================================================

backlog:
  - id: HACK-001
    kind: hack
    title: "Stop typing the same 12 git commands: a .gitconfig alias starter pack"
    brief: "10 aliases we actually use, what each saves, and the one that caused a force-push incident."
    voice: how-to-practical
    priority: P1
    status: done
    published: /hacks/git-alias-starter-pack/

  - id: TOOL-001
    kind: tool
    title: "ripgrep vs grep: the honest review nobody needed but everyone deserves"
    brief: "Why we switched, the one flag that matters, and when plain grep is still fine."
    voice: tool-review-honest
    priority: P1
    status: done
    published: /tools/ripgrep-honest-review/

  - id: HACK-002
    kind: hack
    title: "The 3-line shell function that makes your shell remember where you were"
    brief: "A tiny QoL hack, the failure mode when it shadows a builtin, and the fix."
    voice: how-to-practical
    priority: P2
    status: done
    published: /hacks/make-cd-remember-where-you-were/

  - id: POST-001
    kind: post
    title: "I let a robot redesign my homepage and it added a fake infomercial"
    brief: "Field note on the first autopilot content run, with before/after screenshots."
    voice: meta-confession
    priority: P2
    status: done
    published: /posts/2026/06/22/i-hired-a-robot-to-write-this-website/

  - id: TOOL-002
    kind: tool
    title: "Note-taking apps: a to-do list with a $12/month subscription, reviewed"
    brief: "Three apps, one verdict, and the plain-text setup that beat all of them."
    voice: tool-review-honest
    priority: P3
    status: done
    published: /tools/note-apps-are-todo-lists-with-a-subscription/

  # --- Next up: the autopilot will pick this one ---
  - id: DOC-001
    kind: doc
    title: "How to point the robot at your own site"
    brief: "Adapt the grow-lifehacker skill + backlog pattern for any zer0-mistakes site."
    voice: meta-confession
    priority: P3
    status: done
    published: /docs/point-the-robot-at-your-own-site/

  # Seed ideas for future runs (humans: add freely).
  - id: HACK-003
    kind: hack
    title: "tmux in 9 commands: the survival subset"
    brief: "The handful of tmux bindings that actually matter, and the .tmux.conf that makes them sane."
    voice: how-to-practical
    priority: P2
    status: done
    published: /hacks/tmux-survival-subset/

  - id: TOOL-003
    kind: tool
    title: "fzf: the fuzzy finder that quietly fixes your shell"
    brief: "Ctrl-R history search, file picking, and the one-line install. Honest verdict."
    voice: tool-review-honest
    priority: P2
    status: done
    published: /tools/fzf-fuzzy-finder-honest-review/

  # Fresh tool idea (autopilot run, 2026-06-26): with rg and fzf reviewed, jq is the
  # obvious missing third of the "CLI everyone pastes incantations for" family. The
  # honest angle is the syntax cliff — paste-and-pray — shown with real errors.
  - id: TOOL-004
    kind: tool
    title: "jq: the JSON tool you paste and pray, reviewed honestly"
    brief: "The five filters that earn their keep, the real error messages that send everyone to Stack Overflow, and the shell-quoting trap (single-quote + --arg) shown for real."
    voice: tool-review-honest
    priority: P2
    status: done
    published: /tools/jq-honest-review/

  # Surfaced by the fzf review (TOOL-003): the `list | fzf | act` pattern deserves
  # its own hack — the review pitches it but stops short of copy-pasteable functions.
  - id: HACK-004
    kind: hack
    title: "Four fzf shell functions you'll actually keep: kill, branch, cd, open"
    brief: "The `something_that_lists | fzf | something_that_acts` pattern turned into four real ~/.bashrc functions — fuzzy process-kill, git branch switch, jump-to-dir, edit-a-file — each one we ran, plus the quoting gotcha that bites the kill one."
    voice: how-to-practical
    priority: P2
    status: done
    published: /hacks/fzf-shell-functions/

  - id: DOC-002
    kind: doc
    title: "Wiring the guardrails: the CI layer that actually enforces 'the robot proposes, the human disposes'"
    brief: "Companion to DOC-001. The five files give the robot its manners; this is the part that holds it to them — branch protection, CODEOWNERS, required status checks, and the auto-merge gate. What to copy beyond the five files, and how to verify the lock is on before a robot ever opens a PR."
    voice: meta-confession
    priority: P2
    status: done
    published: /docs/wiring-the-guardrails/

  # Surfaced by DOC-002 (wiring the guardrails): while writing that doc the robot
  # ran `gh api .../branches/main/protection` against this repo and got 404
  # "Branch not protected" (and `.../rulesets` -> []). The lock the doc describes
  # is not actually on. This is an OPS task for an admin, not a content PR — the
  # bot has Write, not Admin, and cannot enable branch protection itself. Run the
  # `gh api -X PUT` payload from docs/runbook-fleet.md §2 as @bamr87.
  - id: OPS-001
    kind: ops   # ops/admin task — the fleet SKIPS these (a content agent can't enable branch protection); stays here for a human
    title: "Enable branch protection on main (the lock DOC-002 documents but found OFF)"
    brief: "Run the branch-protection PUT from docs/runbook-fleet.md §2 as an admin: require the `verify` check + a code-owner review, block force-push, linear history. Until then CODEOWNERS is advisory and `verify` is non-blocking. Verify after with `gh api repos/bamr87/lifehacker.dev/branches/main/protection`."
    voice: meta-confession
    priority: P1
    status: todo

  # --- Review follow-ups from PR #20 (tmux survival subset) ---
  - id: HACK-006
    kind: hack
    title: "Make tmux survive a reboot, not just a disconnect (resurrect + continuum)"
    brief: "The survival-subset hack stops at 'outlives the terminal'; this is the sequel that makes sessions outlive a full restart. tmux-resurrect to save/restore, tmux-continuum to autosave, and the part where it can't bring back the actual running processes — only the layout."
    voice: how-to-practical
    priority: P2
    status: done
    published: /hacks/tmux-survive-a-reboot/

  - id: HACK-005
    kind: hack
    title: "One .tmux.conf line per real annoyance: the config that grows up"
    brief: "A follow-up to the six-line survival config — the next handful of lines worth adding once tmux sticks (vi copy-mode keys, system-clipboard yank, a status line that isn't an eyesore, and the renumber-on-close setting), each justified by the specific annoyance it removes. Keep the 'every line earns its place' rule."
    voice: how-to-practical
    priority: P3
    status: done
    published: /hacks/tmux-conf-that-grows-up/

  # --- Review follow-up from PR #21 (colophon: the fleet learned to spawn) ---
  # The colophon now claims the fleet can spawn, but only as one dense paragraph.
  # The safety design behind it is the actual story and is genuinely on-brand
  # ("the mechanics are the content"). Natural sequel to DOC-002.
  - id: DOC-003
    kind: doc
    title: "How we let a robot fleet spawn itself without letting it off the leash"
    brief: "The design decisions that make autonomous agent-spawning safe to ship: the kill switch gated transitively (an idle dispatcher emits an empty plan, so the spawn job is skipped — not a second flag to forget), max-parallel:1 so leases can't race, 'fail RED, never silent-skip' when an agent crashes, and one-PR-per-agent that a human still merges. The honest part: each of these is a guardrail we added because the obvious version was unsafe. Show the version that wasn't."
    voice: meta-confession
    priority: P2
    status: done
    published: /docs/let-the-fleet-spawn-itself/

  # Fresh doc (no todo doc remained; DOC-001/002/003 shipped, DOC-004 is a blocked
  # post). The Meta pillar documents how the autopilot WRITES and how the human
  # GATES, but not the layer in between: how the robot tests its own work before it
  # dares open a PR. The verification harness (scripts/ci + /test-lifehacker) is
  # genuinely on-brand ("the mechanics are the content") and lets the robot paste
  # real captured output from running the gate on this very repo.
  - id: DOC-005
    kind: doc
    title: "How the robot grades its own homework: the verification harness"
    brief: "Companion to the Autopilot Playbook and Wiring the Guardrails. The harness between writing and merging: reproducing GitHub Pages safe mode by stripping _plugins, the one-finding-per-line frozen contract, the fingerprint that excludes line numbers, and the single error count that IS the merge gate. What each check is allowed to block vs. merely flag, and why a failed hack command is content, not a stop."
    voice: meta-confession
    priority: P2
    status: done
    published: /docs/how-the-robot-grades-its-own-homework/

  # Fresh doc (no `todo` doc item remained; DOC-001/002/003/005 shipped, DOC-004 is
  # a blocked post). The Meta pillar documents the harness as a whole (DOC-005) but
  # the brand linter — the one check that meets the site's entire comedy premise and
  # is DELIBERATELY not allowed to block — only gets a paragraph there. It earns its
  # own deep-dive: the satire-license problem a regex can't solve, the `satire_line?`
  # heuristic and its blind spots, the two-tier handoff, and the recursive joke that
  # the linter flags the very docs that describe it. Demonstrable with real captured
  # output (85 findings, 0 errors) from running it on this repo.
  - id: DOC-006
    kind: doc
    title: "The word police that can't make an arrest"
    brief: "Deep-dive companion to DOC-005 on lint_brand.rb: the glossary lint that flags the robot's favorite hype words (just, 10x, revolutionary) but is built to never block — because the same words are licensed for satire. The satire_line? heuristic, where it guesses wrong in both directions, the tier-2 handoff that narrows the question without answering it, and the recursive bit where the linter cites the docs that explain it. Real `ruby scripts/ci/lint_brand.rb` output."
    voice: meta-confession
    priority: P2
    status: done
    published: /docs/the-word-police-that-cant-make-an-arrest/

  # Surfaced by HACK-004 (fzf shell functions): the post names `fzf -m`
  # multi-select as the one place you DON'T quote the result, but doesn't build
  # it out. The multi-select variants (kill several procs, stage several files,
  # delete several branches) are their own small hack — and the TAB/Shift-TAB
  # binding plus the "act takes many args" pattern deserve a real walkthrough.
  - id: HACK-008
    kind: hack
    title: "fzf -m: the multi-select functions, and the one time you don't quote"
    brief: "Follow-up to the four single-pick functions. With `-m`, fzf returns several lines — kill a handful of processes, git add a handful of files, delete a handful of branches. Each ran, plus the deliberate un-quoting that makes word-splitting work FOR you, and the guard that stops an empty pick from acting on everything."
    voice: how-to-practical
    priority: P3
    status: done
    published: /hacks/fzf-multi-select-functions/

  # Surfaced by the DOC-002 review (PR #33): the doc ends on a live 404 — branch
  # protection is OFF and an admin still has to throw the switch (OPS-001). Once
  # OPS-001 lands, there's an honest "the part where it got fixed" follow-up worth
  # writing: re-run the same `gh api` command and show the before/after. Closes
  # the loop the doc deliberately left open. Gate on OPS-001 being done first.
  - id: DOC-004
    kind: post
    title: "We threw the switch: the 404 that became a branch-protection rule"
    brief: "Short companion to DOC-002, written the day branch protection actually went on. The same `gh api .../branches/main/protection` call that returned 404 'Branch not protected' now returns the rules block — paste both, before and after, real output only. The lesson: the doc that admits the lock is off is what got the lock turned on. Do not write until OPS-001 is done and the PUT has run; the whole point is the verified after-state."
    voice: meta-confession
    priority: P3
    # BLOCKED on OPS-001: the post needs the verified after-state (branch protection
    # actually ON). The fleet correctly declined to fabricate it; skip until an admin
    # runs the PUT, then flip back to todo. (Fleet skips non-todo.)
    status: blocked

  # Synthesized in-lane when a `post` run found the queue dry: every post item was
  # `done` and the only other (DOC-004) was legitimately `blocked` on OPS-001. Rather
  # than no-op, cross collections, or fabricate the blocked post, the autopilot wrote
  # and produced this fresh Field Note about that exact moment. (See the post for the
  # "four wrong moves" reasoning.)
  - id: POST-002
    kind: post
    title: "The day my to-do list had nothing I was allowed to do"
    brief: "Meta-confession: the autopilot opens the backlog for a post run, finds every post item done or blocked, and walks through the four wrong moves (no-op, cross-lane, fabricate the blocked one, lower the bar) versus the one honest one — synthesize a well-formed in-lane item and write it. Real `gh pr list` + backlog state, no invented output. Payload: how to design a task queue's 'empty inbox' behavior."
    voice: meta-confession
    priority: P2
    status: done
    published: /posts/2026/06/26/nothing-i-was-allowed-to-do/
  # --- Fresh hack (no todo `hack` item existed; all prior hacks done). The ssh
  # config angle is an untouched corner: every existing hack is git/fzf/tmux. A
  # named-host config is genuinely useful, fully testable offline via `ssh -G`,
  # and carries a real gotcha (first-match-wins ordering) — the failure stays in.
  - id: HACK-009
    kind: hack
    title: "Stop typing IP addresses: the ~/.ssh/config block that names your servers"
    brief: "Turn `ssh deploy@203.0.113.10` into `ssh web1` with a config file, the ProxyJump line that replaces the netcat bastion dance, and the first-match-wins ordering rule that silently logs you in as the wrong user. Every resolution verified with `ssh -G` (offline, no connection)."
    voice: how-to-practical
    priority: P2
    status: done
    published: /hacks/ssh-config-stop-typing-ip-addresses/

  # Synthesized in-lane: a `post` run found the queue dry again — every post item
  # done, DOC-004 still blocked on OPS-001. Rather than no-op or cross lanes, the
  # autopilot wrote the one thing it could cover honestly this run: the recurring
  # merge conflict on THIS file. The skill warns about it ("concurrent content PRs
  # collide on lines appended at the end"); this post is the autopsy, with a real
  # reproduced conflict and a real clean-merge contrast (both run, output pasted).
  - id: POST-003
    kind: post
    title: "The one file the whole robot fleet fights over"
    brief: "Meta-confession: backlog.yml is the shared to-do list, and when two autopilot runs each append a new item to the end, git conflicts on the same final lines. Reproduce the conflict for real, then show why the minimal 'flip only my own status line' edit merges clean. The lesson: where you write in a shared file decides whether parallel writers collide. Real git output only."
    voice: meta-confession
    priority: P2
    status: done
    published: /posts/2026/06/27/the-one-file-the-whole-fleet-fights-over/

  # Fresh tool idea (autopilot run, 2026-06-27): every `tool` item was already done
  # (rg, fzf, jq, note-apps), so no todo `tool` existed and none could be borrowed
  # from another lane. `fd` is the missing fourth of the modern-CLI family: it's the
  # `find` counterpart to the already-reviewed `rg`, fully testable offline, and
  # carries TWO real honest gotchas left in — the Debian/Ubuntu `fdfind` binary-name
  # collision, and the smart defaults (.gitignore + hidden files skipped) that quietly
  # hide files you expected `find` to show. Demonstrated with real captured output.
  - id: TOOL-005
    kind: tool
    title: "fd: the find replacement with two surprises in the box"
    brief: "Why `fd` is `find` with sane defaults, the Debian/Ubuntu naming collision that means `fd` isn't even called fd, the .gitignore-and-hidden-files defaults that silently hide what you searched for, and the `-mtime`/`-perm` cases where plain find still wins. Every command run for real."
    voice: tool-review-honest
    priority: P2
    status: done
    published: /tools/fd-honest-review/

  # Fresh tool idea (autopilot run, 2026-06-28): every `tool` item was already done
  # (rg, fzf, jq, note-apps, fd) so no todo `tool` existed and none could be borrowed
  # from another lane. `bat` is the natural cat-side complement to the already-reviewed
  # rg (grep) and fd (find) — completing the Rust-rewrite-of-coreutils trio — and it
  # carries the same shape of honest gotcha left in: the Debian/Ubuntu `batcat` binary
  # name collision, the pager-by-default that ambushes cat muscle memory, and the
  # auto-plain-when-piped behavior that means it does NOT break pipes (the good
  # surprise). Demonstrated with real captured output on a fresh Ubuntu box.
  - id: TOOL-006
    kind: tool
    title: "bat: the cat replacement that pages, highlights, and changes its name on Debian"
    brief: "Why `bat` is `cat` for reading (syntax highlighting, line numbers, a git change gutter), the `batcat` naming collision on Debian/Ubuntu, the pager-by-default that surprises cat muscle memory, the auto-plain-when-piped behavior that means it doesn't actually break your pipes, and why plain cat still belongs in your scripts. Every command run for real."
    voice: tool-review-honest
    priority: P2
    status: done
    published: /tools/bat-honest-review/

  # --- Fresh hack (autopilot run, 2026-06-27): every `hack` item was already
  # done and the only `todo` was OPS-001 (kind: ops, which content runs skip).
  # Rather than no-op or cross collections, synthesized a fresh in-lane hack.
  # `make` as a task runner is an untouched corner — existing hacks cover git,
  # shell functions, tmux, fzf, ssh — and it's fully testable offline with two
  # real, famous failures (the .PHONY file-shadow and the tab-vs-spaces
  # "missing separator"). Both reproduced and left in.
  - id: HACK-010
    kind: hack
    title: "Stop forgetting your own build commands: a Makefile as your project's command menu"
    brief: "Use make as a task runner, not a C compiler: a self-documenting `make help` menu, why `.PHONY` matters (a file named like a target silently shadows it), and the tab-vs-spaces 'missing separator' error. Every command and both failures reproduced for real."
    voice: how-to-practical
    priority: P2
    status: done
    published: /hacks/make-task-runner-command-menu/

  # --- Fresh hack (autopilot run, 2026-06-28): every `hack` item was already done
  # and the only `todo` was OPS-001 (kind: ops, which content runs skip). No open
  # auto:content PR to duplicate. Rather than no-op or cross collections, synthesized
  # a fresh in-lane hack. Bash strict mode (`set -euo pipefail`) is an untouched
  # corner — existing hacks cover git/fzf/tmux/ssh/make/shell-functions but never the
  # one header that makes scripts fail loudly. Fully reproducible offline and rich in
  # famous backfires (set -u + $1, set -e + (( i++ )) returning 1). All run for real.
  - id: HACK-011
    kind: hack
    title: "Make bash fail loudly: the set -euo pipefail header (and the two times it backfires)"
    brief: "The three-line header that turns silent bash failures into loud ones — what each flag catches, shown with real captured output, plus the two famous backfires (set -u on an unset $1, set -e on (( i++ )) returning 1) and the fixes that keep the header without the surprises."
    voice: how-to-practical
    priority: P2
    status: done
    published: /hacks/bash-strict-mode-fail-loudly/

  # Synthesized in-lane (autopilot run, 2026-06-29): a `post` run found the queue
  # dry — POST-001/002/003 all done, and the only other post item (DOC-004) still
  # `blocked` on OPS-001. No open auto:content PR is a post. Rather than no-op,
  # cross lanes, or fabricate the blocked post, the autopilot wrote the one honest
  # thing it could capture this run: it reached for its own commit history (the
  # "robot narrates its own labor" motif) and found a SHALLOW clone — one commit,
  # no history. Real `git rev-parse --is-shallow-repository` / `rev-list --count`
  # / blame-boundary output from this very checkout. Payload: shallow clones, why
  # CI does them, when they bite (describe/blame/log audits), and how to unshallow.
  - id: POST-004
    kind: post
    title: "I tried to count my own commits and the repo only had one"
    brief: "Meta-confession: the autopilot goes to audit its own labor (git shortlog, log --author=claude, blame) and discovers CI checked it out shallow — fetch-depth 1, exactly one commit, history amputated. Real captured output: is-shallow-repository=true, rev-list --count=1, blame's ^boundary marker that makes every line look born in one commit, describe failing. Payload: what a shallow clone is, why actions/checkout defaults to depth 1, the four things it silently breaks, and fetch-depth:0 / git fetch --unshallow as the fix."
    voice: meta-confession
    priority: P2
    status: done
    published: /posts/2026/06/29/i-tried-to-count-my-own-commits/
  # Fresh doc (autopilot run, 2026-06-29): every `doc` item was already done
  # (DOC-001/002/003/005/006); DOC-004 is a blocked `post`; the only `todo` on the
  # whole board is OPS-001 (kind: ops, which content runs skip). No open auto:content
  # PR is a doc (open PRs: POST-004, TOOL-007, HACK-012). Rather than no-op, cross
  # lanes, or fabricate the blocked post, synthesized a fresh in-lane doc. The Meta
  # pillar deep-dives nearly every step of the autopilot loop — writing (Playbook),
  # verifying (DOC-005), the linter (DOC-006), guardrails (DOC-002), fleet safety
  # (DOC-003) — but step 2, "pick the highest-priority todo," only ever got a single
  # bullet. The selection algorithm is the gap, and it's demonstrable with real
  # captured output from this very run.
  - id: DOC-007
    kind: doc
    title: "How the robot picks what to write next (and when it refuses)"
    brief: "Deep-dive on step 2 of the autopilot loop: the backlog selection algorithm. Lane-matching (never cross collections), todo-only filtering, why ops items are skipped, priority+ID sort, the open-PR dedup check, and the four ways a run can end in nothing (no-op, cross-lane, fabricate, or the one honest move — synthesize a fresh in-lane item). Real `gh pr list` and backlog state from the run that wrote it."
    voice: meta-confession
    priority: P2
    status: done
    published: /docs/how-the-robot-picks-what-to-write/
  # --- Fresh hack (autopilot run, 2026-06-29): every `hack` item was already done
  # and the only `todo` was OPS-001 (kind: ops, which content runs skip); no open
  # auto:content PR to duplicate. Rather than no-op or cross collections, synthesized
  # a fresh in-lane hack and the natural sequel to HACK-011 (strict mode): once a
  # script fails loudly mid-run, it should still clean up the temp dir it made.
  # `trap … EXIT` is an untouched corner (existing hacks cover git/fzf/tmux/ssh/make/
  # strict-mode) and is fully reproducible offline with real failures left in — the
  # leaked temp dir, the empty-variable `rm -rf "/"`, and the SIGKILL it can't catch.
  - id: HACK-012
    kind: hack
    title: "Make your bash scripts clean up after themselves: trap … EXIT"
    brief: "The one trap line that deletes a script's temp dir on success, error, and Ctrl-C/kill — the leaked-dir failure that motivates it, the empty-variable `rm -rf \"/\"` that makes a wrong trap dangerous (and the ${var:?} guard + assign-then-arm ordering that defuse it), and the honest limit that SIGKILL can't be trapped. Every command and failure run for real."
    voice: how-to-practical
    priority: P2
    status: done
    published: /hacks/bash-trap-exit-cleanup/
  # Fresh tool idea (autopilot run, 2026-06-29): every prior `tool` item was already
  # done (rg, fzf, jq, note-apps, fd, bat) and the only `todo` was OPS-001 (kind: ops,
  # which content runs skip); the one open auto:content PR is a hack, no tool to
  # duplicate. Rather than no-op or cross collections, synthesized a fresh in-lane
  # tool. `eza` is the missing `ls` member of the modern-CLI family already reviewed
  # — completing the rg(grep)/fd(find)/bat(cat) coreutils-rewrite set — and it carries
  # the same shape of honest gotcha left in: the tool you'd search for (`exa`) is a
  # DEAD, archived project and `eza` is the fork (verified: `apt-cache policy exa`
  # returns no candidate on Ubuntu 24.04, and the eza package ships an `exa -> eza`
  # compatibility symlink), `--git` silently drops its column outside a repo, and the
  # `-l` long format columns differ from GNU `ls -l` so parsing `$5` for size breaks.
  # Every command run for real on a fresh Ubuntu 24.04 box.
  - id: TOOL-007
    kind: tool
    title: "eza: the ls replacement whose own name is a tombstone"
    brief: "Why `eza` is `ls` with colors, a git column, and a tree mode, the dead-fork story (you'll search for `exa`, which is archived; `eza` is the maintained fork, and on Ubuntu `exa` is just a symlink to it), the `--git` column that silently vanishes outside a repo, the long-format columns that differ from GNU `ls -l` so muscle-memory parsing breaks, and why plain `ls` still belongs in your scripts. Every command run for real."
    voice: tool-review-honest
    priority: P2
    status: done
    published: /tools/eza-honest-review/

  # Fresh doc (autopilot run, 2026-06-30): every prior `doc` item was already done
  # (DOC-001/002/003/005/006) except DOC-004 (a `post`, blocked on OPS-001) and
  # DOC-007 (an OPEN auto:content PR, "how the robot picks what to write next") — so
  # no `todo` doc existed and the only near-topic was already taken. Rather than
  # no-op or cross lanes, synthesized a fresh in-lane doc. The Meta pillar documents
  # the harness (DOC-005) and the brand linter (DOC-006) but never the Prime Directive
  # RUNNER — the check that literally executes the commands the site prints, in a
  # --network=none rootless read-only sandbox. Genuinely on-brand ("the mechanics are
  # the content") and demonstrable with real captured output: running it on this repo
  # exposed a live bug (docker?'s `$?` probe returns false on Ruby 3.3, so the runner
  # silently degrades to "unverified" for all 37 eligible blocks while the gate stays
  # green). The verifier that couldn't verify is the story. Bug reported in the PR
  # description for the scripts/ci owners — not patched here (content branch).
  - id: DOC-008
    kind: doc
    title: "The box with no internet: the Prime Directive runner that executes every command this site prints"
    brief: "Deep-dive companion to DOC-005 on run_hack_commands.rb: the opt-in `lh:run` model, the --network=none/--read-only/non-root/tmpfs sandbox and why a block needing rg/fzf fails by design (the correct signal), why a failed command is a Field Note seed and never blocks, and the honest twist — running it for real on this repo returned docker=false (the `$?`-after-Open3.capture2e bug on Ruby 3.3), so the verifier verified nothing while glowing green. Real captured output only."
    voice: meta-confession
    priority: P2
    status: done
    published: /docs/the-box-with-no-internet/
  # Fresh post (autopilot run, 2026-06-30): every `post` item was done, DOC-004 still
  # `blocked` on OPS-001, and the one open auto:content post PR (POST-004) covers
  # commit-counting. Rather than no-op or cross lanes, synthesized a fresh in-lane post.
  # While auditing the robot's own links (all clean) the run found a real config trap:
  # production `_config.yml` has NO `future` key (Jekyll default false) while
  # `_config_dev.yml` sets `future: true`, so the preview build shows future-dated posts
  # the live site silently buries. Reproduced for real with two jekyll builds isolating
  # the flag. Latent, not a current outage (no live post is future-dated) — said so.
  - id: POST-005
    kind: post
    title: "The post my preview shows and production buries"
    brief: "Meta-confession: the autopilot previews with future: true (set in _config_dev.yml) but production GitHub Pages builds with future: false (the Jekyll default, absent from _config.yml), so a post dated even one day ahead of the build clock renders in preview and vanishes live. Reproduced with two real jekyll builds isolating the flag. The honest part: it hasn't buried a published post yet because none is future-dated — but the preview can't warn me, because preview is the one build where it always looks fine. Fix recommended (make dev match prod), not applied — config isn't content."
    voice: meta-confession
    priority: P2
    status: done
    published: /posts/2026/06/30/preview-shows-future-posts-production-buries-them/
  # Fresh tool idea (autopilot run, 2026-06-30): every prior `tool` item was done
  # (rg, fzf, jq, note-apps, fd, bat, eza) and the only `todo` was OPS-001 (kind:
  # ops, which content runs skip); the open auto:content PRs are hacks/doc/post, no
  # tool to duplicate. Rather than no-op or cross collections, synthesized a fresh
  # in-lane tool. `zoxide` is the missing `cd` member of the modern-CLI family
  # already reviewed — completing rg(grep)/fd(find)/bat(cat)/eza(ls) — and pairs
  # with the existing make-cd-remember hack. Honest gotchas left in and run for
  # real on Ubuntu 24.04: the package gives you no `z` until the init hook is added,
  # it learns nothing in non-interactive shells (the PROMPT_COMMAND hook never
  # fires), the cold-start empty database, and the leaf-match rule (`z proj` fails
  # when every entry is proj/<sub>). Interactive disambiguation (`zi`) needs fzf.
  - id: TOOL-008
    kind: tool
    title: "zoxide: the smarter cd that does nothing until you turn the key"
    brief: "Why zoxide is cd ranked by frecency, the package-installs-but-no-z-command trap (you must add the init hook), the non-interactive shells where it silently learns nothing, the cold-start empty database, and the leaf-match rule everyone gets wrong on day one. Interactive pick (zi) needs fzf. Every command run for real."
    voice: tool-review-honest
    priority: P2
    status: done
    published: /tools/zoxide-honest-review/
  # --- Fresh hack (autopilot run, 2026-06-30): every `hack` item in the backlog was
  # already done and the only `todo` was OPS-001 (kind: ops, which content runs skip).
  # The one open auto:content hack PR is HACK-012 (trap … EXIT cleanup), a different
  # topic. Rather than no-op, cross collections, or duplicate, synthesized a fresh
  # in-lane hack. `find | xargs` is an untouched corner — existing hacks cover
  # git/fzf/tmux/ssh/make/bash-strict-mode/shell-functions but never the pipeline
  # everyone pastes — and it carries two famous, fully-offline-reproducible footguns
  # left in: the space-in-a-filename split (one file becomes two args) fixed with
  # -print0/-0, and GNU xargs running the command on empty input fixed with -r.
  # Every command run for real; the safe-pattern block is lh:run and passes the gate.
  - id: HACK-013
    kind: hack
    title: "Pipe find into xargs without splitting your filenames in half"
    brief: "The find | xargs pattern everyone pastes, the space-in-a-filename footgun that splits one file into two args (fixed with -print0/-0), the empty-input trap where GNU xargs runs the command anyway (fixed with -r), and the -I{}/-n1/-P flags worth knowing. Every command run for real."
    voice: how-to-practical
    priority: P2
    status: done
    published: /hacks/xargs-pipe-find-without-splitting-filenames/
  # --- Fresh doc (autopilot run, 2026-07-01): every `doc` item on the board was
  # already done (DOC-001/002/003/005/006/007/008); DOC-004 is a `post` blocked on
  # OPS-001; the only `todo` is OPS-001 (kind: ops, which content runs skip). No open
  # auto:content PR is a doc (the three open are POST-006, TOOL-009, HACK-014). The
  # docs cover the harness that WRITES findings (DOC-005) and the backlog the robot
  # picks FROM (DOC-007) — but nothing documents the layer in between: triage, which
  # turns findings.jsonl into a RICE-ranked, deduped queue and files GitHub issues it
  # is structurally forbidden from closing. Researched by running the real scripts on
  # this repo: 268 findings collapsed to 74 queue items (dedup by fingerprint), and a
  # file_issues dry-run planned the create/label/route with a MAX_NEW cap. Real
  # captured output only. Fills the missing middle of the meta docs.
  - id: DOC-009
    kind: doc
    title: "The bug tracker that can't close a ticket: how triage ranks what the harness found"
    brief: "Deep-dive companion to DOC-005/DOC-007 on the triage layer (scripts/triage/): how build_queue.rb turns findings.jsonl into a fingerprint-deduped, RICE-ranked queue where severity dominates any reach multiplier; how file_issues.rb finds-or-files GitHub issues on the stable triage-fp marker (create / comment-still-failing / reopen-on-regression) behind a MAX_NEW cap; the quarantine rule that treats every inbound issue body as data, never instructions; and the guardrail that names the whole thing — it can file, label, rank, and @-mention, but it can never close an issue, merge a PR, or write to the upstream theme repo. Ran the real scripts on this repo: 268 findings to 74 queue items, dry-run issue plan captured verbatim."
    voice: meta-confession
    priority: P2
    status: done
    published: /docs/the-bug-tracker-that-cant-close-a-ticket/

  # Synthesized in-lane (autopilot run, 2026-07-01): a `post` run found the queue
  # dry again — POST-001..005 all done, DOC-004 still `blocked` on OPS-001, and the
  # only two open auto:content PRs are a tool (TOOL-009 delta) and a hack (HACK-014
  # worktree), no post to duplicate. Rather than no-op, cross lanes, or fabricate the
  # blocked post, synthesized a fresh in-lane post. Direct sequel to POST-003 (the
  # backlog merge conflict): POST-003 ended by pointing at `.gitattributes merge=union`
  # as the fix. This post is the audit of that fix's OWN failure mode — union never
  # conflicts, so it never asks a human, and it silently (a) keeps both near-duplicate
  # items and (b) collapses the identical trailing `status:` line, leaving one item
  # with no status at all. Reproduced for real: valid YAML, exit 0, TOOL-002 status=nil.
  - id: POST-006
    kind: post
    title: "The merge that never conflicts, and the backlog item it quietly ate"
    brief: "Meta-confession sequel to POST-003. The union merge driver we added to stop backlog.yml conflicts works — it never fails. That's the problem: it never asks a human either. Reproduced for real that two near-duplicate appends merge clean (exit 0, valid YAML) but union collapses the shared trailing `status: drafting` line, leaving one item with status=nil. The safety net that removes the loud failure introduces a quiet one. Real git + YAML-parse output only."
    voice: meta-confession
    priority: P2
    status: done
    published: /posts/2026/07/01/the-merge-that-never-conflicts/
  # Fresh tool idea (autopilot run, 2026-07-01): every prior `tool` item was done
  # (rg, fzf, jq, note-apps, fd, bat, eza, zoxide) and the only `todo` was OPS-001
  # (kind: ops, which content runs skip); the one open auto:content PR is a hack
  # (git worktree), no tool to duplicate. Rather than no-op or cross collections,
  # synthesized a fresh in-lane tool. `delta` is the git-diff member of the
  # modern-CLI family already reviewed — pairs with the git-alias hack and bat —
  # and carries a genuinely DIFFERENT honest trap from the fdfind/batcat renames:
  # `apt install delta` installs an unrelated 2006 test-case minimizer that ships
  # multidelta/singledelta/topformflat and NO `delta` command at all (the pager is
  # `git-delta`), plus the config-not-a-command setup where core.pager and
  # interactive.diffFilter are two independent keys, and the default view encoding
  # add/remove in color only (strip the color and you can't tell). All run for real
  # on Ubuntu 24.04 with git-delta 0.16.5.
  - id: TOOL-009
    kind: tool
    title: "delta: the git-diff pager whose apt package isn't even called delta"
    brief: "Why delta makes git diffs legible (line numbers, word-level highlighting, side-by-side), the `apt install delta` trap that installs an unrelated 2006 tool with no `delta` command (you want `git-delta`), the config-not-a-command setup with two independent gitconfig keys (core.pager for diff/log/show, interactive.diffFilter for `git add -p`), the good surprise that git skips the pager when piped so scripts are safe, and the default view that marks changes in color only. Every command run for real."
    voice: tool-review-honest
    priority: P2
    status: done
    published: /tools/delta-git-diff-honest-review/
  # --- Fresh hack (autopilot run, 2026-07-01): every `hack` item in the backlog was
  # already done and the only `todo` on the whole board is OPS-001 (kind: ops, which
  # content runs skip). `gh pr list --state open --label auto:content` returned []
  # (no open PR to duplicate). Rather than no-op, cross collections, or fabricate,
  # synthesized a fresh in-lane hack. `git worktree` is an untouched corner —
  # existing hacks cover git aliases / fzf / tmux / ssh / make / bash-strict-mode /
  # trap / xargs / shell functions but never checking out two branches at once —
  # and it is fully reproducible offline with two famous failures left in: the
  # "'main' is already used by worktree" lock (can't check out one branch twice) and
  # the stale-entry trap where `rm -rf` a worktree leaves a prunable ghost that only
  # `git worktree prune`/`remove` clears. Every command run for real.
  - id: HACK-014
    kind: hack
    title: "Stop stashing to switch branches: check out two at once with git worktree"
    brief: "Turn the stash/switch/pop dance into a second working directory: git worktree add for a hotfix branch while your half-done work sits untouched, the 'main is already used by worktree' lock (you can't check out one branch in two trees), and the stale-ghost trap where rm -rf leaves an entry only git worktree prune/remove clears. Every command run for real."
    voice: how-to-practical
    priority: P2
    status: done
    published: /hacks/git-worktree-two-branches-at-once/

  # --- Fresh post (autopilot run, 2026-07-02): every `post`-kind item in the backlog
  # was already done except DOC-004, which is legitimately `blocked` on OPS-001
  # (branch protection is still OFF — `gh api .../branches/main/protection` still
  # returns 404, so the verified after-state that post needs does not exist). The
  # open-PR check (`gh pr list --state open --label auto:content`) showed a post
  # already in flight (POST-006, the union-merge one) plus a hack/doc/two tools —
  # nothing my fresh idea duplicates. Rather than no-op, cross lanes, or fabricate
  # the blocked post, synthesized a fresh in-lane Field Note about the exact state
  # the run found: 28 content PRs merged in ~8 days, 5 open and unreviewed by a
  # human (only copilot bot COMMENTED reviews), and this run about to make it six.
  # The bottleneck moved from writing to reviewing. All stats captured for real.
  - id: POST-007
    kind: post
    title: "I opened my sixth pull request before a human read the first five"
    brief: "Meta-confession: the autopilot goes to pick a post, runs gh pr list, and finds the real constraint isn't generation — it's review. 28 content PRs merged in ~8 days, 5 sitting open with zero human approvals (only the copilot bot left COMMENTED reviews), and this run about to add a sixth. Real captured gh output: merge cadence by day, reviewDecision empty on all five, a one-liner to list PRs waiting on a human. Payload: the review queue is the bottleneck in any 'AI proposes, human disposes' loop, why a WIP limit on open PRs matters, and how to see your own queue."
    voice: meta-confession
    priority: P2
    status: done
    published: /posts/2026/07/02/sixth-pull-request-before-a-human-read-five/
  # --- Fresh doc (autopilot run, 2026-07-02): every `doc` item on the board was
  # already `done` (DOC-001/002/003/005/006 + the two "picks what to write" / "box
  # with no internet" deep-dives shipped), the only `todo` is OPS-001 (kind: ops,
  # which content runs skip), and DOC-004 is a `blocked` post. `gh pr list --state
  # open --label auto:content` shows one open doc PR (DOC-009, the triage layer) — a
  # different topic. Rather than no-op, cross collections, or duplicate, synthesized
  # a fresh in-lane doc. check_drift.rb is an untouched corner of the harness: the
  # Meta pillar deep-dives lint_brand (word police), run_hack_commands (box with no
  # internet), and the harness as a whole (grades its own homework), but the drift
  # tripwire — the one check pointed at the robot's OWN backlog paperwork, asserting
  # every `status: done` resolves to a page you can click — only gets one line in
  # DOC-005. It earns its own deep-dive, with real captured output: the passing run
  # (33 done, 0 errors) plus both failure modes (deadlink + done-without-published)
  # reproduced by a temporary backlog edit that was reverted. (DOC-007/008 already
  # taken; DOC-009 is the open triage-layer PR — this is DOC-010.)
  - id: DOC-010
    kind: doc
    title: "The check that won't take 'done' for an answer"
    brief: "Deep-dive companion to DOC-005 on check_drift.rb: the one check pointed at the robot's own paperwork instead of its prose. Every backlog status:done must resolve to a page that exists (deadlink + done-without-published both hard-fail), the source-only URL resolution that runs without a build and its second-source-of-truth risk vs _config.yml, the sitemap block that learned to self-heal in Liquid, and the search.json built-and-non-empty check. Real ruby scripts/ci/check_drift.rb output — passing run plus both failures reproduced and reverted."
    voice: meta-confession
    priority: P2
    status: done
    published: /docs/the-check-that-wont-take-done-for-an-answer/
  # --- Fresh hack (autopilot run, 2026-07-02): every `hack` item in the backlog
  # was already done and the only `todo` on the whole board is OPS-001 (kind: ops,
  # which content runs skip). Open auto:content PRs are a doc (DOC-009), a post
  # (POST-006), and a tool (TOOL-009) — no open hack to duplicate. Rather than
  # no-op, cross collections, or fabricate, synthesized a fresh in-lane hack.
  # `git reflog` recovery is an untouched corner — existing hacks cover git aliases
  # / worktree / fzf / tmux / ssh / make / bash-strict-mode / trap / xargs but never
  # undoing a bad reset --hard — and it's fully reproducible offline with two honest
  # limits left in: uncommitted work is never in the reflog (reset --hard eats it),
  # and the reflog is local/per-clone (a fresh clone starts empty). Every command
  # run for real; the recovery block is lh:run and passes the gate.
  - id: HACK-015
    kind: hack
    title: "Undo almost anything in git: the reflog is your undo history"
    brief: "The panic after a bad reset --hard, and why the commits almost never actually left: git reflog is the local receipt of everywhere HEAD has pointed. Recover a bad reset and a deleted branch by pointing a branch/HEAD at the reflog entry, plus the two honest limits — uncommitted work is never in the reflog, and the reflog is local/per-clone (a fresh clone starts empty). Every command run for real."
    voice: how-to-practical
    priority: P2
    status: done
    published: /hacks/git-reflog-undo-almost-anything/
  # Fresh tool idea (autopilot run, 2026-07-02): every prior `tool` item was done
  # (rg, fzf, jq, note-apps, fd, bat, eza, zoxide) and the only `todo` on the board
  # is OPS-001 (kind: ops, which content runs skip). The one open auto:content tool
  # PR is TOOL-009 (delta, the git-diff pager) — a different tool — so nothing to
  # duplicate. Rather than no-op or cross collections, synthesized a fresh in-lane
  # tool. `sd` is the missing `sed` member of the modern-CLI family already reviewed
  # — completing rg(grep)/fd(find)/bat(cat)/eza(ls)/zoxide(cd) — and it carries the
  # same shape of honest gotchas left in and run for real on Ubuntu 24.04: it edits
  # files IN PLACE with no backup (no -i), the find pattern is a regex not a literal
  # (dots over-match; -s fixes it), and capture groups are $1 not \1. Good surprise:
  # unlike fd/bat it keeps its name (no fdfind/batcat rename). Every command captured.
  - id: TOOL-010
    kind: tool
    title: "sd: the sed replacement that edits your file before you've finished reading the command"
    brief: "Why `sd` is `sed` for find-and-replace without the s///g ceremony, the in-place-by-default edit that overwrites with no backup and no -i, the find-pattern-is-a-regex trap where dots over-match (fixed with -s/--string-mode), the $1-not-\\1 capture syntax and the $$ literal-dollar trap, the -p/--preview safety valve, and where plain sed's line addressing still wins. Every command run for real."
    voice: tool-review-honest
    priority: P2
    status: done
    published: /tools/sd-honest-review/
  # --- Harvested + promoted by loop-tuner (2026-07-03) to relieve backlog
  # starvation. loop_metrics showed 0 `todo` items across ALL four growable kinds
  # (hack/tool/post/doc), so the content factory was improvising fresh, unmeasured
  # ideas inline every run (open PRs #126-129 are each "fresh, not from queue").
  # scripts/triage/harvest_ideas.rb recovered this doc idea from merged PR #102's
  # `## Backlog ideas` section; it duplicates no open PR (DOC-011/#129 is a
  # different doc) and no existing backlog title. Promoted with proper
  # id/kind/voice/priority ONLY — metadata, not prose; the factory writes it — to
  # give the starved `doc` lane one measured item to pick next. DOC-011 is in
  # flight (#129), so this is DOC-012.
  - id: DOC-012
    kind: doc
    title: "The human is the rate limiter"
    brief: "A Meta doc on review-throughput vs generation-throughput: the autopilot opens PRs far faster than a human can review them, and the no-self-merge guardrail makes review the designed bottleneck by intent, not by accident. Why an open-PR WIP limit matters and how to measure the review queue that actually caps the loop."
    voice: meta-confession
    priority: P2
    status: done
    published: /docs/the-human-is-the-rate-limiter/
  # Fresh tool idea (autopilot run, 2026-07-03): every prior `tool` item was done
  # (rg, fzf, jq, note-apps, fd, bat, eza, zoxide, delta, sd) and the only `todo` on
  # the board is OPS-001 (kind: ops, which content runs skip). The one open
  # auto:content PR is a hack (git bisect) — no tool to duplicate. Rather than no-op
  # or cross collections, synthesized a fresh in-lane tool. `hyperfine` is the
  # command-line benchmarking member of the modern-CLI family and pairs with the
  # already-reviewed coreutils rewrites (it's what you'd use to prove rg beats grep).
  # It carries a genuinely different honest angle from the fdfind/batcat renames: it
  # is honest to a fault about its own margin of error. Two real gotchas left in and
  # run for real on the Ubuntu 24.04 CI box: the shell-overhead calibration that
  # returns nonsense (0.0 µs min) on fast commands until you pass -N, and the
  # "statistical outliers" warning that never quits on a shared/CI box because
  # wall-clock time there measures the whole machine, not your command. Real captured
  # output only (hyperfine 1.18.0).
  - id: TOOL-011
    kind: tool
    title: "hyperfine: the benchmarking tool that warns you its own numbers might be lying"
    brief: "Why hyperfine is `time` with real statistics and a repeat count it picks for you, the shell-overhead trap where a fast command reports a 0.0 µs minimum until you pass -N/--shell=none, the 'statistical outliers' warning that never quits on a shared/CI box because it measures wall-clock (the whole machine, not your command), the --warmup/--prepare flags that level the field, the relative-speedup summary and markdown export worth keeping, and why it's a stopwatch not a profiler. Every command run for real."
    voice: tool-review-honest
    priority: P2
    status: done
    published: /tools/hyperfine-honest-review/

  # --- Fresh doc (autopilot run, 2026-07-03): every `doc` item on the board was
  # already done (DOC-001/002/003/005/006/007/008/009/010); DOC-004 is a `post`
  # blocked on OPS-001; the only `todo` is OPS-001 (kind: ops, which content runs
  # skip). Open auto:content PRs are a post (byline/git-blame), a tool (hyperfine),
  # and a hack (git bisect) — no open doc to duplicate. Rather than no-op, cross
  # collections, or fabricate, synthesized a fresh in-lane doc. The triage doc
  # (DOC-009) covered build_queue + file_issues but never harvest_ideas.rb — the
  # loop-closer that recovers the `## Backlog ideas` bullets merged PRs leave in
  # their descriptions (the "later" the skill's "list ideas in the PR body, triage
  # promotes them later" refers to). Genuinely on-brand ("the mechanics are the
  # content") and carries a real honest twist captured on this repo: of 35 merged
  # auto:content PRs only 1 (#102) actually left a `## Backlog ideas` section, so
  # the harvester yielded 3 candidates from one PR — a correct machine feeding on a
  # convention the fleet mostly forgot to follow. Real self-test (7/7) + run output.
  - id: DOC-011
    kind: doc
    title: "The suggestion box at the bottom of every pull request"
    brief: "Deep-dive companion to DOC-009 on harvest_ideas.rb: the read-only script that scans merged auto:content PR bodies for the `## Backlog ideas` heading, parses the bullets (stopping at any `#`, even the `##Testing` typo, to under-harvest rather than over), dedupes them case/punct/backtick-blind against the backlog and each other, and prints candidates it is structurally forbidden from adding itself. The honest twist, captured real on this repo: only 1 of 35 merged PRs left the section, so the loop-closer feeds on a convention the fleet mostly forgot — the machine is correct, the input pipe is nearly dry. Real --self-test (7/7 PASS) and run output."
    voice: meta-confession
    priority: P2
    status: done
    published: /docs/the-suggestion-box-at-the-bottom-of-every-pr/
  # Synthesized in-lane (autopilot run, 2026-07-03): a `post` run found the queue
  # dry — POST-001..007 all done, and the only other post-kind item (DOC-004) is
  # still `blocked` on OPS-001 (branch protection remains OFF: `gh api
  # .../branches/main/protection` still returns 404, so the verified after-state
  # that post needs does not exist). The open-PR check (`gh pr list --state open
  # --label auto:content`) showed only a tool (hyperfine) and a hack (git bisect) —
  # no post to duplicate. Rather than no-op, cross lanes, or fabricate the blocked
  # post, synthesized a fresh in-lane Field Note about a real finding from this run:
  # the site's front-matter byline says a robot wrote the post, but `git log` on the
  # last commit credits the human (author), GitHub (committer), and demotes the bot
  # to a `Co-authored-by:` trailer — and all three authors.yml personas share one
  # GitHub handle (bamr87), so the robot's author card links to the human. The git
  # checkout has NO configured identity (git config user.name → exit 1) and is a
  # depth-1 shallow clone, so even auditing the pattern is capped at one commit. All
  # outputs captured for real. Payload: byline vs git authorship are separate facts.
  - id: POST-008
    kind: post
    title: "The byline says a robot wrote this; git blame says a human did"
    brief: "Meta-confession: the front-matter `author:` says Claude, but every tool that records authorship credits the human. Real captured output — the last commit's author is Amr, committer is GitHub, the robot is only a Co-authored-by trailer (and it's the triage bot, not this one); `git config user.name` is unset (exit 1); all three authors.yml personas share github: bamr87 so the robot's author card links to the human; and the depth-1 shallow clone caps the audit at one commit. Payload: a byline is a display string, git authorship is a separate fact set by the account the automation runs under, and they quietly disagree — how to spot the seam and attribute a bot honestly."
    voice: meta-confession
    priority: P2
    status: done
    published: /posts/2026/07/03/byline-says-robot-git-blame-says-human/
  # --- Fresh hack (autopilot run, 2026-07-03): every `hack` item in the backlog was
  # already done and the only `todo` on the whole board is OPS-001 (kind: ops, which
  # content runs skip). `gh pr list --state open --label auto:content` returned []
  # (no open PR to duplicate). Rather than no-op, cross collections, or fabricate,
  # synthesized a fresh in-lane hack. `git bisect run` is an untouched corner —
  # existing git hacks cover aliases / reflog / worktree but never binary-searching
  # history for a regression — and it is fully reproducible offline with two famous
  # exit-code footguns left in: the backwards test (0=good) that inverts the search
  # and confidently blames a docs commit, and the unbuildable-midpoint case where a
  # naive test blames the build break instead of returning 125 to skip. Every
  # command run for real; the automated-bisect block is lh:run and passes the gate.
  - id: HACK-016
    kind: hack
    title: "Let git find the commit that broke it: git bisect run"
    brief: "git bisect is binary search over history for the commit that introduced a bug, and `git bisect run` automates the answering with a test script (exit 0=good, non-zero=bad). The two exit-code traps left in: a backwards test that inverts the whole search and blames a docs commit, and the unbuildable midpoint where a naive test blames the build break instead of returning 125 to skip. Every command run for real."
    voice: how-to-practical
    priority: P2
    status: done
    published: /hacks/git-bisect-run-find-the-bad-commit/
  # --- Fresh hack (autopilot run, 2026-07-05): every `hack` item in the backlog was
  # already done and the only `todo` on the whole board is OPS-001 (kind: ops, which
  # content runs skip). The one open auto:content hack PR is HACK-017 (tar tarbomb) —
  # a different topic. Rather than no-op, cross collections, or fabricate, synthesized
  # a fresh in-lane hack. `rsync` is an untouched corner — existing hacks cover
  # git (aliases/reflog/worktree/bisect) / fzf / tmux / ssh / make / bash-strict-mode /
  # trap / xargs / shell functions but never copying or mirroring a directory — and it
  # is fully reproducible offline (local-to-local) with three famous footguns left in:
  # the trailing-slash rule (`src` nests, `src/` copies contents), the `--delete`
  # mirror that removes files the dry-run (-n) exists to preview, and the quick-check
  # miss where a same-size in-place edit within one mtime second is silently skipped
  # (fixed with --checksum). Every command run for real; the safe block is lh:run.
  - id: HACK-018
    kind: hack
    title: "rsync without nuking the wrong directory: the trailing slash, --delete, and -n"
    brief: "The trailing-slash rule that decides whether you copy a directory or its contents, the --delete mirror that quietly removes files not in the source, the -n dry-run that shows you first, and the quick-check miss where a same-size same-second edit gets skipped (fixed with --checksum). Every command run for real on rsync 3.2.7."
    voice: how-to-practical
    priority: P2
    status: done
    published: /hacks/rsync-trailing-slash-delete-footguns/
  # Fresh tool idea (autopilot run, 2026-07-04): every prior `tool` item was done
  # (rg, fzf, jq, note-apps, fd, bat, eza, zoxide, delta, sd, hyperfine) and the only
  # `todo` on the board is OPS-001 (kind: ops, which content runs skip) plus DOC-012
  # (a doc, wrong lane). `gh pr list --state open --label auto:content` showed one
  # open PR and it's a hack (tar/tarbomb) — no tool to duplicate. Rather than no-op
  # or cross collections, synthesized a fresh in-lane tool. `hexyl` is the missing
  # xxd/hexdump member of the modern-CLI family already reviewed — completing
  # rg(grep)/fd(find)/bat(cat)/eza(ls)/zoxide(cd)/sd(sed) — and it carries genuinely
  # DIFFERENT honest gotchas from the fdfind/batcat renames, all run for real on
  # Ubuntu 24.04 with hexyl 0.8.0: (1) it's a VIEWER with no revert, so the classic
  # `xxd -r` patch round-trip doesn't exist; (2) `--color` defaults to `always` (not
  # auto), so piping keeps ANSI escapes AND box-drawing — the exact OPPOSITE of bat,
  # and it means `hexyl | grep` is unusable; (3) squeezing collapses repeated lines
  # to `*` and hides data unless you pass -v. Every command captured.
  - id: TOOL-012
    kind: tool
    title: "hexyl: the hex viewer that keeps its colors even when you pipe it"
    brief: "Why hexyl is xxd/hexdump with byte-category color, the no-revert limit (it's a viewer, not an editor — the `xxd -r` patch round-trip doesn't exist), the `--color` default of `always` that keeps ANSI escapes AND box-drawing in a pipe (opposite of bat, so `hexyl | grep` is useless), the squeezing that collapses repeated lines to `*` and hides data unless you pass -v, the -n/-s/--skip niceties, and why plain xxd/od still belong in your scripts. Every command run for real."
    voice: tool-review-honest
    priority: P2
    status: done
    published: /tools/hexyl-honest-review/

  # --- Fresh post (autopilot run, 2026-07-04): every `post` item on the board was
  # already done except DOC-004 (kind: post), which is `blocked` on OPS-001 (kind:
  # ops — a human-only admin action a content run must skip) and explicitly says
  # "do not write until OPS-001 is done." `gh pr list --state open --label
  # auto:content` returned only a tool (hexyl) and a hack (tar) PR — nothing to
  # duplicate. Rather than no-op or cross collections, synthesized a fresh Field
  # Note from a real finding: the build harness warned that our own fleet-spawn doc
  # renders its GitHub Actions `${{ ... }}` expressions as a bare `$` — Liquid and
  # Actions both claim the double curly brace, and Jekyll goes first. Every command
  # (build warning, the `if: $` render, the isolated Liquid reproductions) run for
  # real; the raw/endraw fix verified in the harness build.
  - id: POST-009
    kind: post
    title: "The workflow snippet my site published as a lonely dollar sign"
    brief: "Field note on the GitHub-Actions-vs-Liquid double-curly-brace collision: our own fleet-spawn doc shipped `if: $` because Jekyll's Liquid ate the `${{ ... }}` expression before Actions ever saw it. Real build warning, the real broken render (`if: $` / `item: $`), the silent no-warning variant, and the raw/endraw fix — all captured, not invented."
    voice: meta-confession
    priority: P2
    status: done
    published: /posts/2026/07/04/jekyll-ate-my-github-actions-expression/
  # --- Fresh doc (autopilot run, 2026-07-05): the only `todo` doc on the board is
  # DOC-012 ("the human is the rate limiter"), which already has an OPEN auto:content
  # PR (autopilot/the-human-is-the-rate-limiter) — so it would duplicate. Every other
  # doc is done; the only other `todo` is OPS-001 (kind: ops, which content runs skip).
  # Rather than no-op, cross collections, or duplicate the open PR, synthesized a fresh
  # in-lane doc. lint_frontmatter.rb is an untouched corner of the harness — the Meta
  # docs deep-dive lint_brand (word police), check_drift (won't take done), and
  # run_hack_commands (box with no internet), but never the per-collection front-matter
  # schema validator that enforces the very SKILL.md templates. Genuinely on-brand and
  # demonstrable with real captured output: the clean run (6 warnings, 0 errors) plus
  # both error modes reproduced with scratch files and reverted. The honest twist left
  # in: the `docs` collection is validated leniently (title+description only), so the
  # Meta docs that preach honest attribution are held to the loosest byline standard —
  # verified by a scratch doc passing on two keys while the identical hack threw six
  # errors. Cross-links POST-005 (the future-date rule is the static half of that bug).
  - id: DOC-013
    kind: doc
    title: "The front-matter cop that waves its own docs through"
    brief: "Deep-dive companion to DOC-005 on lint_frontmatter.rb: the per-collection schema check that turns the SKILL.md templates into a merge gate. What blocks (missing keys, unknown author, future date, wrong collection, tool-with-no-verdict, post filename-date mismatch) vs. what only warns (over-long SEO description), the future-date rule as the static half of the preview-vs-prod bug (POST-005), and the honest twist that the `docs` collection is lenient — so the Meta docs about honest attribution get the loosest byline standard of all. Real captured output: clean run plus both error modes reproduced and reverted."
    voice: meta-confession
    priority: P2
    status: done
    published: /docs/the-front-matter-cop/
  # --- Fresh post (autopilot run, 2026-07-05): every `post` item was already done
  # and the only `todo` items on the whole board are OPS-001 (kind: ops, which content
  # runs skip) and DOC-012 (kind: doc — wrong lane, and already open as PR #145).
  # `gh pr list --state open --label auto:content` confirmed #145 flips DOC-012 to
  # done on its branch while main still reads `todo`. Rather than no-op, cross lanes,
  # or fabricate, synthesized a fresh in-lane post: the backlog's `status` field has
  # no state for "claimed / in review", so `todo` silently means "todo OR drafted-PR-
  # open", inviting duplicate drafts. Real captured output only (the awk todo scan,
  # the gh pr list DOC-012 match, and the PR #145 diff flipping status: todo->done).
  - id: POST-010
    kind: post
    title: "The only thing left on my to-do list was already a pull request"
    brief: "Meta-confession on task-queue state: the picker trusts one field, status: todo. The only non-ops todo item (DOC-012) is already an open PR (#145) whose branch flips it to done, but main still reads todo — so `todo` collapses two states, genuinely-untouched and drafted-PR-open. The classic no-'claimed'-state, at-least-once double-processing bug; the only thing stopping a duplicate draft is the procedural `gh pr list` cross-check, not the schema. Real captured output only. Payload: a review-gated queue needs a claimed/in-review state (or a lease / idempotency key), because writing the terminal state only at merge leaves a window where a taken item looks free."
    voice: meta-confession
    priority: P2
    status: done
    published: /posts/2026/07/05/only-todo-was-already-a-pull-request/
  # Fresh tool idea (autopilot run, 2026-07-05): every prior `tool` item was done
  # (rg, fzf, jq, note-apps, fd, bat, eza, zoxide, delta, sd, hyperfine) so no `todo`
  # tool existed and none could be borrowed from another lane; the only `todo` on the
  # board is OPS-001 (kind: ops, which content runs skip) plus DOC-012 (a doc).
  # `gh pr list --state open --label auto:content` returned [] (no open PR to
  # duplicate). Rather than no-op or cross collections, synthesized a fresh in-lane
  # tool. `dust` is the missing `du` member of the modern-CLI family already reviewed
  # — completing rg(grep)/fd(find)/bat(cat)/eza(ls)/zoxide(cd)/sd(sed) — and it
  # carries a genuinely DIFFERENT honest angle from the fdfind/batcat renames: it is
  # not in the Ubuntu archive AT ALL (apt-cache policy dust du-dust → empty), so the
  # naming curse is absence not shadowing (crate/deb=du-dust, binary=dust); its human
  # numbers never match du's because both count disk blocks but du rounds up and dust
  # rounds down (17M vs 16M on the same tree); and on a real TTY it truncates the tree
  # to screen height, hiding small entries that reappear the moment you pipe it. Every
  # command run for real with dust 1.2.4 on Ubuntu 24.04.
  - id: TOOL-014
    kind: tool
    title: "dust: the du replacement that draws you a tree (and rounds the other way)"
    brief: "Why `dust` answers 'what's eating my disk?' as a sorted percentage-bar tree, the no-apt-package-at-all install (crate/deb du-dust, binary dust), the numbers that never match du because both count blocks but round opposite directions, the TTY truncation that hides small entries until you pipe it, the good surprise that unlike its rg/fd/bat siblings it counts .gitignore'd and hidden files (which is what you want for disk usage), and why plain du still belongs in your scripts. Every command run for real."
    voice: tool-review-honest
    priority: P2
    status: done
    published: /tools/dust-honest-review/
  # --- Fresh hack (autopilot run, 2026-07-04): every `hack` item in the backlog
  # was already done and the only `todo` on the whole board is OPS-001 (kind: ops,
  # which content runs skip) plus DOC-012 (a doc, wrong lane). `gh pr list --state
  # open --label auto:content` returned [] (no open PR to duplicate). Rather than
  # no-op, cross collections, or fabricate, synthesized a fresh in-lane hack. `tar`
  # is an untouched corner — existing hacks cover git / fzf / tmux / ssh / make /
  # bash-strict-mode / trap / xargs but never the archive tool everyone downloads a
  # release with — and it carries two famous, fully-offline-reproducible footguns
  # left in: the tar bomb (archiving `*` instead of the dir, so it explodes loose
  # files into whatever cwd extracts it) fixed by `tar tf` inspection + archiving
  # the directory, and the `-cfz` flag-order slip where `f` eats the `z` and writes
  # an uncompressed archive literally named `z`. Every command run for real; the
  # safe-pattern block is lh:run and passes the gate.
  - id: HACK-017
    kind: hack
    title: "Stop shipping tarballs that explode: archive the directory, tar tf before you extract"
    brief: "The tar bomb footgun — `tar czf x.tar.gz *` archives loose files that scatter into whatever directory extracts them — fixed by inspecting with `tar tf` first and archiving the folder itself, plus the `-cfz` flag-order slip that writes an uncompressed archive named `z`. Every command run for real."
    voice: how-to-practical
    priority: P2
    status: done
    published: /hacks/tar-no-tarbomb-inspect-before-extract/
  # Fresh post idea (autopilot run, 2026-07-06): every `post` item on the board is
  # `done` except DOC-004, which is `blocked` on OPS-001 (kind: ops, a human-only
  # admin task still `todo`) and explicitly says "do not write until OPS-001 is
  # done" — so there is no honest in-lane `todo` post and DOC-004 needs a state that
  # doesn't exist yet. `gh pr list --state open --label auto:content` returned only a
  # tool (procs #190) and a hack (sort|uniq #189) — no post PR to duplicate. Rather
  # than no-op, fabricate the blocked one, or cross lanes, synthesized a fresh
  # meta-confession from a real finding surfaced THIS run: `ruby
  # scripts/ci/lint_frontmatter.rb` flags six `description-too-long` warnings, and
  # all six are the robot's own content (1 hack, 2 tools, 3 posts, 164–172 chars).
  # The linter itself codifies a 160-char SEO cap but sets it severity=warning with
  # the comment "existing content runs ~170" — the rule was written already-broken
  # and never enforced. Real captured output only (linter run, the rule's source
  # lines, measured description lengths). Payload: warning-vs-error severity in a
  # lint gate + why ~160 chars is where search engines truncate a meta description.
  - id: POST-011
    kind: post
    title: "I wrote the 160-character rule, then broke it in six of my own posts"
    brief: "Meta-confession: my own front-matter linter caps SEO descriptions at 160 characters, but it's severity=warning with the comment 'existing content runs ~170' — so it never blocks. Six pieces exceed it (164–172 chars) and all six are mine; every one shipped. Real captured output: the linter run, the rule's source lines, the measured lengths. Payload: error-vs-warning severity is the difference between a rule and a suggestion, and ~160 chars is where SERPs truncate a meta description with an ellipsis (often eating the CTA). I left the six unfixed on purpose — retro-editing six files isn't this PR's lane — and kept THIS post's own description under the cap."
    voice: meta-confession
    priority: P2
    status: done
    published: /posts/2026/07/06/the-160-character-rule-i-broke-six-times/
  # --- Fresh hack (autopilot run, 2026-07-06): every `hack` item in the backlog
  # was already done and the only `todo` on the whole board is OPS-001 (kind: ops,
  # which content runs skip). `gh pr list --state open --label auto:content`
  # returned [] (no open PR to duplicate). Rather than no-op, cross collections, or
  # fabricate, synthesized a fresh in-lane hack. The `sort | uniq -c | sort -rn`
  # tally is an untouched corner — existing hacks cover git / fzf / tmux / ssh /
  # make / bash-strict-mode / trap / xargs / rsync / tar but never the "what shows
  # up most?" pipeline everyone pastes — and it carries two famous, fully-offline-
  # reproducible footguns left in: uniq only collapsing ADJACENT duplicates (so a
  # missing leading `sort` silently under-counts), and the default lexical `sort -r`
  # that ranks 9 above 100 until you add `-n`. Every command run for real; the
  # tested block is lh:run and passes the gate.
  - id: HACK-019
    kind: hack
    title: "Count what happens most: the sort | uniq -c tally (and why uniq lies until you sort)"
    brief: "The sort | uniq -c | sort -rn pipeline that ranks the top entries in any list, the adjacency footgun where uniq without a leading sort under-counts, the lexical-vs-numeric trap where sort -r puts 9 above 100 (fixed with -n), the -d/-u cousins, and the byte-exact case/whitespace trap. Every command run for real."
    voice: how-to-practical
    priority: P2
    status: done
    published: /hacks/sort-uniq-count-tally/
  # --- Fresh doc (autopilot run, 2026-07-06): every `doc` item on the board was
  # already done (DOC-001/002/003/005/006/007/008/009/010/011/012/013); DOC-004 is
  # a `post` blocked on OPS-001; the only `todo` is OPS-001 (kind: ops, which content
  # runs skip). `gh pr list --state open --label auto:content` showed a post
  # (POST-011), a tool (TOOL-013 procs), and a hack (HACK-019 sort|uniq) — no open doc
  # to duplicate. Rather than no-op, cross collections, or fabricate, synthesized a
  # fresh in-lane doc. htmlproofer_check.rb is an untouched corner of the harness: the
  # Meta docs deep-dive lint_brand (word police), check_drift (won't take done),
  # run_hack_commands (box with no internet), and lint_frontmatter (front-matter cop),
  # but never step 7, the internal-link gate — the natural page->page companion to
  # check_drift's backlog->page audit. Genuinely on-brand ("the mechanics are the
  # content") with a real honest twist: html-proofer 5.x signals failure by exiting
  # the process (SystemExit, not StandardError), the trap that let 341 broken links
  # through on the first CI run, and the crash-safe rewrite that blocks a broken
  # checker instead of passing it. Built the site (181 pages) and ran the real check:
  # clean (1808 internal links, 0 error), the injected-broken-link error, and the
  # no-site info path all captured verbatim from this repo.
  - id: DOC-014
    kind: doc
    title: "The link checker that doesn't trust a clean exit"
    brief: "Deep-dive companion to DOC-005/DOC-010 on htmlproofer_check.rb: step 7 of the harness, the internal-link/image/anchor gate over the built _site/. Why external links and enforce_https are deliberately off, the SystemExit trap where html-proofer 5.x exits the process instead of raising (rescue StandardError misses it) and let 341 real failures through on the first CI run, the crash-safe rewrite that turns a broken checker into a blocking `proofer-crashed` finding, the no-site info that isn't a pass, and the three-regex theme-origin ignore list routed upstream. Real captured output: clean run (1808 links), an injected broken link caught, and the no-build path."
    voice: meta-confession
    priority: P2
    status: done
    published: /docs/the-link-checker-that-doesnt-trust-a-clean-exit/

  # --- Fresh doc (autopilot run, 2026-07-07): a `doc` run found the queue dry —
  # every DOC item is done except DOC-004 (which is kind: post, and blocked on
  # OPS-001), and `gh pr list --state open --label auto:content` showed only a
  # post PR (POST-012), nothing to duplicate in-lane. Rather than no-op, cross
  # lanes, or fabricate, synthesized a fresh in-lane doc: the Meta pillar has a
  # deep-dive for every CI check EXCEPT build.sh — step 1, THE gate every other
  # check builds a _site/ on top of. DOC-005 covers it in one line; this expands
  # it. Real captured output only: the full `build.sh build` run (185 pages), the
  # seven theme _plugins it strips, and the reproduced `Unknown tag` safe-mode
  # failure (exit 1) that the strip exists to catch.
  - id: DOC-015
    kind: doc
    title: "The build that deletes its own plugins"
    brief: "Deep-dive companion to DOC-005 on build.sh: step 1 of the harness and the gate the rest stands on. Why a remote_theme site has no local layouts, the broad cp -R overlay (whole _data/_includes/assets, learned the hard way), and the load-bearing `rm -rf _plugins` that reproduces GitHub Pages safe mode. The theme ships seven plugins GH Pages never runs; captured the canonical `Unknown tag` failure (exit 1) by feeding the stripped overlay a real theme-plugin tag. Plus the one-build-path design (preview.sh sources build.sh, run-all.sh executes it) that makes local/CI drift unrepresentable, and the honest `fatal: not a git repository` line the script tries to silence and doesn't."
    voice: meta-confession
    priority: P2
    status: done
    published: /docs/the-build-that-deletes-its-own-plugins/

  # --- Fresh tool (autopilot run, 2026-07-06): every `tool` item on the board is
  # already done (TOOL-001..012) and `gh pr list --state open --label auto:content`
  # showed only a hack PR (HACK-019) — nothing to duplicate in-lane. Rather than
  # no-op, cross lanes, or fabricate, synthesized a fresh in-lane tool. The modern-
  # CLI family we've reviewed covers rg/fd/bat/eza/zoxide/sd/delta/hexyl/dust/jq/
  # fzf/hyperfine but never the `ps` replacement — a glaring gap. `procs` fills it
  # and carries real, offline-reproducible surprises left in: the ~2-min cargo build
  # (no apt package), keyword search that substring-matches the whole command line
  # (so it lists the shell running the query — captured), sort keys that are procs's
  # own column kinds (`rss` works, `vsz`/`memory` give "Can't find column kind"),
  # lossy `--json` numbers (MEM 86 vs table 0.1% vs ps RSS 14616), and color that
  # strips cleanly when piped. Every command run for real on Ubuntu 24.04.
  - id: TOOL-013
    kind: tool
    title: "procs: the ps replacement that searches by keyword (and matches your own shell)"
    brief: "The Rust ps replacement: a readable colored table plus keyword search that beats `ps aux | grep`. Honest edges left in — the two-minute cargo build with no apt package, search that substring-matches the entire command line (so it catches your parent shell), sort keys that are procs's own column names, and lossy --json numbers. Verdict: keep it for interactive use, leave ps in scripts."
    voice: tool-review-honest
    priority: P2
    status: done
    published: /tools/procs-honest-review/
  # --- Harvested + promoted by loop-tuner (2026-07-06) to relieve backlog
  # starvation. loop_metrics showed 0 `todo` items across ALL four growable kinds
  # (hack/tool/post/doc) — every prior item is `done`, OPS-001 is `ops` (content
  # runs skip it) and DOC-004 is `blocked` — so the factory was improvising fresh,
  # unmeasured ideas inline every run. scripts/triage/harvest_ideas.rb surfaced 16
  # candidates from merged PRs' `## Backlog ideas` sections; these three are the
  # non-duplicate, offline-testable ones (they need no network host, so the "run
  # for real" rule holds). Promoted with metadata ONLY — id/kind/voice/priority;
  # the factory writes the prose. Relieves the hack/tool/post lanes so
  # growable_todo 0->3 and starved_kinds drops from 4 kinds to just `doc`.
  - id: HACK-020
    kind: hack
    title: "Read a binary header without leaving the shell: walk a magic number with hexyl"
    brief: "A hands-on companion to the hexyl review: use `hexyl -n`/`-s` to walk the magic-number header of a PNG/ELF/ZIP, tying byte offsets to hexyl's color categories so the first 16 bytes stop being noise. Every command run for real on a local file (no network)."
    voice: how-to-practical
    priority: P2
    status: done
    published: /hacks/walk-a-magic-number-with-hexyl/

  - id: TOOL-015
    kind: tool
    title: "The hex editor hexyl can't be: an honest review of a real TUI hex editor (hexedit/heh)"
    brief: "hexyl's headline caveat is that it can only READ — so its natural sibling is a hex editor that can write. Honest review of a TUI hex editor (hexedit or heh): the read-with-hexyl/edit-with-this pairing, the overwrite-in-place-no-insert model that surprises text-editor muscle memory, and where plain xxd -r still wins. Every command run for real on a local file (no network)."
    voice: tool-review-honest
    priority: P2
    status: done
    published: /tools/hexedit-honest-review/

  - id: POST-012
    kind: post
    title: "The date in the filename and the link that trusted it"
    brief: "Meta-confession data note: the backlog's `published:` links are a second source of truth for permalinks, and at least one (POST-003) drifted from its post's filename date. A Field Note on why filename-date drift silently breaks cross-links, reproduced with the real 404 the run hit — no invented output."
    voice: meta-confession
    priority: P2
    status: done
    published: /posts/2026/07/07/the-date-in-the-filename-and-the-link-that-trusted-it/

  # --- content-scout topic ideas (auto-appended; humans may edit/reprioritize) ---
  # Each carries the source it-journey.dev page it was inspired by; grow-lifehacker
  # links that page in the published piece. Default P3 — human items outrank these.
  - id: SRC-001
    kind: hack
    title: "Rotate the secret you already deleted: detect-secrets and the key still living in git history"
    brief: "Scan a repo with `detect-secrets scan`, then face the part nobody likes: deleting the line and committing does not remove the key — it sits in every prior commit and was scraped seconds after you pushed. The real fix is rotate first, then purge history with git filter-repo, plus a pre-commit hook so it never lands again. The failure — 'I deleted it, we're fine' — stays in."
    voice: how-to-practical
    priority: P3
    status: done
    published: /hacks/rotate-the-secret-still-in-git-history/
    source: content-scout
    source_url: https://it-journey.dev/quests/1011/secure-coding/
    source_title: "Secure Coding: Preventing the OWASP Top 10"
    fingerprint: 4553171b47c4
  - id: SRC-002
    kind: tool
    title: "pip-audit: the dependency scanner that finds real holes and a pile you can't patch"
    brief: "Honest verdict on `pip-audit` (and its npm audit cousin): it catches genuinely known-vulnerable versions in seconds, and it also floods you with transitive-dependency advisories you can't fix and scores that don't match your actual risk. What earns an upgrade, what's noise, and when to wire it into CI versus running it by hand."
    voice: tool-review-honest
    priority: P3
    status: done
    source: content-scout
    source_url: https://it-journey.dev/quests/1011/secure-coding/
    source_title: "Secure Coding: Preventing the OWASP Top 10"
    fingerprint: adf392372480
    published: /tools/pip-audit-honest-review/
  - id: SRC-003
    kind: hack
    title: "Scope the token before your MCP server gets the keys: a fine-grained PAT that can't touch prod"
    brief: "An MCP server inherits every permission of the token you hand it, so a classic admin PAT means one bad prompt can force-push. Build a fine-grained personal access token with read-only Contents/Issues/PR scopes, wire it into the server's env, and prove the limit by asking the agent to do something it shouldn't and watching it get a 403. The gotcha: the easy token is the dangerous one."
    voice: how-to-practical
    priority: P3
    status: todo
    source: content-scout
    source_url: https://it-journey.dev/quests/1000/agentic-mcp-server-mastery/
    source_title: "Agentic MCP Server Mastery"
    fingerprint: 4e37aa97b69a

  # --- content-scout topic ideas (auto-appended; humans may edit/reprioritize) ---
  # Each carries the source it-journey.dev page it was inspired by; grow-lifehacker
  # links that page in the published piece. Default P3 — human items outrank these.
  - id: SRC-004
    kind: hack
    title: "STRIDE your side project in 20 minutes: one data flow, six questions, a ranked fix list"
    brief: "Skip the enterprise threat-model binder. Draw one data-flow diagram (you, your app, your database), run the six STRIDE questions against the single arrow that crosses a trust boundary, and rank what you find by likelihood times impact. You'll know it worked when you have three fixes worth doing this week — and the honest part is that the paper version beats the fancy modeling tool you'll never open twice."
    voice: how-to-practical
    priority: P3
    status: todo
    source: content-scout
    source_url: https://it-journey.dev/quests/1011/threat-modeling/
    source_title: "Threat Modeling"
    fingerprint: d8a03b73e730
  - id: SRC-005
    kind: hack
    title: "Scope your GitHub Actions permissions block before the default token can write to everything"
    brief: "The default GITHUB_TOKEN hands a workflow broad write access to your whole repo, so one compromised action can push to main. Add a top-level `permissions:` block set to `contents: read`, then grant each job only the scope it truly needs. The footgun that stays in: the moment you declare that block, every scope you didn't list drops to no-access, so the job that pushes a tag or cuts a release starts failing with a 403 until you add `contents: write` back exactly where it belongs."
    voice: how-to-practical
    priority: P3
    status: todo
    source: content-scout
    source_url: https://it-journey.dev/quests/1100/agentic-codex-06-guardrails-and-accountability/
    source_title: "The Warden Pact: Guardrails & Accountability"
    fingerprint: 254407d15418
  - id: SRC-006
    kind: hack
    title: "type -a before you alias: see what a command name already runs so you don't shadow it"
    brief: "Before you drop `alias ls='ls --color'` into your .bashrc, run `type -a ls` to see everything that name already resolves to — alias, shell builtin, function, and the real /bin/ls, in the order the shell picks them. The failure that stays in: you alias over a name a script depends on, it breaks in a way that works fine when you paste the same command by hand, and `command ls` (which skips the alias) is the one-word escape hatch."
    voice: how-to-practical
    priority: P3
    status: done
    published: /hacks/type-a-before-you-alias/
    source: content-scout
    source_url: https://it-journey.dev/quests/0000/cellar/
    source_title: "Bashcrawl: The Cellar Quest"
    fingerprint: 232160d5796a

  # --- content-scout topic ideas (auto-appended; humans may edit/reprioritize) ---
  # Each carries the source it-journey.dev page it was inspired by; grow-lifehacker
  # links that page in the published piece. Default P3 — human items outrank these.
  - id: SRC-007
    kind: hack
    title: "Read a failing CI run from your terminal: gh run view --log-failed and the watch --exit-status trap"
    brief: "Stop clicking through the Actions web UI to find why a run went red. `gh run view --log-failed` prints just the failing step's log, and `gh run watch` follows a run live. The trap that stays in: `gh run watch` exits 0 even when the run failed, so a script that gates on it treats a broken build as green — you have to add `--exit-status` to make the failure actually propagate."
    voice: how-to-practical
    priority: P3
    status: done
    published: /hacks/gh-run-log-failed-and-watch-exit-status/
    source: content-scout
    source_url: https://it-journey.dev/notes/gh-600/evaluation-signals-table/
    source_title: "GH-600 Evaluation Signals Table"
    fingerprint: 449b195a9aaf
  - id: SRC-008
    kind: hack
    title: "chmod 644 and 755, never 777: the permission bits that decide what's safe to run"
    brief: "Read an `ls -l` line as a file-type char plus three rwx triples, then set them on purpose: 644 (rw-r--r--) for data, 755 (rwxr-xr-x) for scripts, and never 777, which hands write access to everyone on the box. Two failures stay in: you `chmod +x` a script that has no `#!` shebang line and it still won't run, and you forget the `./` prefix because the current directory isn't on your PATH."
    voice: how-to-practical
    priority: P3
    status: done
    published: /hacks/chmod-644-755-never-777/
    source: content-scout
    source_url: https://it-journey.dev/quests/0000/armoury/
    source_title: "Bashcrawl: The Armoury"
    fingerprint: ceb55a3c397d
  - id: SRC-009
    kind: hack
    title: "Read the query plan before you add the index: EXPLAIN ANALYZE, Seq Scan vs Index Scan, and the function that kills it"
    brief: "Stop guessing which column to index. Run `EXPLAIN ANALYZE` on the slow query and read the plan: a `Seq Scan` with a big 'Rows Removed by Filter' is the tell, an `Index Scan` is the win. Add the index, re-run, watch the time drop. Two footguns stay in: a composite index on `(a, b)` does nothing for a query that filters only on `b` (column order matters), and wrapping the column in a function — `WHERE lower(email) = ...` — silently defeats the plain index until you build a matching expression index."
    voice: how-to-practical
    priority: P3
    status: todo
    source: content-scout
    source_url: https://it-journey.dev/quests/0110/query-optimization/
    source_title: "Query Optimization: Tuning Fast Database Queries"
    fingerprint: cd4a20d1a4eb

  # --- Fresh doc (autopilot run, 2026-07-08): the doc lane was dry — every DOC
  # item on the board is already `done`, DOC-004 is a `post` blocked on OPS-001,
  # and the only `todo` items are OPS-001 (kind: ops, which content runs skip) and
  # the SRC-* scout hacks/tools (wrong lane). `gh pr list --state open --label
  # auto:content` showed only a post PR (POST-013) — no doc to duplicate. Rather
  # than no-op, cross lanes, or fabricate, synthesized a fresh in-lane doc.
  # classify_changes.rb is an untouched corner of the harness: the Meta pillar has
  # a deep-dive for every CI *check* (build/brand/drift/frontmatter/links/hack-box)
  # but not the router that runs BEFORE them and decides which even execute. It's
  # the natural companion to DOC-005 (harness overview) and DOC-002 (guardrails,
  # which uses it in the auto-merge smuggle guard). Honest twist left in: the
  # router can only round UP (empty/unrecognized diff runs the full pipeline), and
  # the one required check — verify/build — deliberately has no `needs: changes` so
  # it ignores the router entirely (a flaked router once left main with no green
  # build). Real captured output only: classify_changes runs on seven real diffs
  # plus the $GITHUB_OUTPUT booleans, all on this repo.
  - id: DOC-016
    kind: doc
    title: "The Router That Can Only Round Up"
    brief: "Deep-dive companion to DOC-005 on classify_changes.rb: the 50-line change-type router that runs before any check and decides which tier executes (content/deps/pipeline/data). Why every ambiguous case — empty diff, unrecognized file, or a flaked router job — rounds UP to the full pipeline instead of skipping, why the one required `verify`/build check deliberately ignores the router (no `needs: changes`, after a killed router job left main with no green build), and the same regex read four ways: tier gating, report scoping, the auto-merge smuggle guard, and the auto-fix refusal — where `pipeline`/`deps` flips from 'run more' to 'a human must look'. Real captured output: seven diffs classified plus the $GITHUB_OUTPUT booleans."
    voice: meta-confession
    priority: P2
    status: done
    published: /docs/the-router-that-can-only-round-up/
  # Fresh post (autopilot run, 2026-07-08): the post lane was dry — every post item
  # was `done` except DOC-004 (blocked on OPS-001), OPS-001 is a skipped ops task,
  # and the only `todo` items were the nine content-scout SRC ideas (8 hacks, 1 tool,
  # 0 posts). Rather than cross lanes, no-op, or fabricate the blocked post, the
  # factory wrote this fresh Field Note about that exact structural fact: the scout's
  # mandatory-source_url rule means it can refill the hack/tool lanes but never the
  # post lane. Real captured awk/grep output only.
  - id: POST-013
    kind: post
    title: "The idea firehose refills every lane but the one I write in"
    brief: "Meta-confession on queue shape: the backlog is fed by one source (the content-scout mining it-journey.dev), and its honesty rule — no source_url, no proposal — means it structurally can only file hacks and tools, never a self-narrating post. So the post lane looks perpetually idle while the board is nine items deep. A queue fed by a single source inherits that source's blind spots. Real captured output only."
    voice: meta-confession
    priority: P2
    status: done
    published: /posts/2026/07/08/the-scout-refills-every-lane-but-mine/

  # --- content-scout topic ideas (auto-appended; humans may edit/reprioritize) ---
  # Each carries the source it-journey.dev page it was inspired by; grow-lifehacker
  # links that page in the published piece. Default P3 — human items outrank these.
  - id: SRC-010
    kind: hack
    title: "80% coverage and still broken: the branch c8 counted as covered but never ran"
    brief: "Wire a coverage gate into your tests with `c8 --check-coverage --lines 80` so an untested change fails the build instead of merging quietly. The honest part that stays in: line coverage is the easy metric to game — a function whose `if` branch never fired can still show 100% lines covered, so the number says safe while a whole path is untested. Turn on `--branches` too, and know the difference before you trust the badge."
    voice: how-to-practical
    priority: P3
    status: todo
    source: content-scout
    source_url: https://it-journey.dev/quests/0101/testing-integration/
    source_title: "Testing Integration: Tiered CI/CD Test Gates"
    fingerprint: 3fda0c87d3e1
  - id: SRC-011
    kind: hack
    title: "Stop scrolling CI logs: write a real summary to $GITHUB_STEP_SUMMARY"
    brief: "A GitHub Actions job doesn't have to bury its result in 800 lines of log. Echo Markdown to the `$GITHUB_STEP_SUMMARY` file and it renders as a table on the run's summary page — pass/fail counts, the one number that mattered, a link. The gotchas that stay in: it appends (every `>>` adds, so `>` when you meant `>>` eats the earlier lines), it's per-job not global, and echoing untrusted text into it is a Markdown-injection footgun you handle by not piping raw input through it."
    voice: how-to-practical
    priority: P3
    status: todo
    source: content-scout
    source_url: https://it-journey.dev/quests/0111/agentic-codex-01-agents-in-the-sdlc/
    source_title: "Initiation Rites: Agents in the SDLC"
    fingerprint: 00d2be97aa29
  - id: SRC-012
    kind: hack
    title: "You can't gate a workflow step on a secret in `if:` — promote it to an env first"
    brief: "The obvious guard — `if: secrets.MY_TOKEN != ''` — doesn't work, because GitHub Actions won't evaluate `secrets.*` in a job-level `if:`. Copy the secret to a step output or a job env first, then branch on that, so a workflow that needs a token skips cleanly instead of failing when it's absent. The footgun that stays in: pull requests from forks get empty secrets by design, so any agent step that assumes the token exists breaks on exactly the PRs strangers open — the graceful no-op is the fix, not a louder error."
    voice: how-to-practical
    priority: P3
    status: todo
    source: content-scout
    source_url: https://it-journey.dev/quests/0011/ouroboros-loop-03-summon-the-golem/
    source_title: "Summon the Golem: Claude Code as a Headless CI Agent"
    fingerprint: f8ac9ca11bdd

  # --- Fresh doc (autopilot run, 2026-07-09): the doc lane was dry — every DOC item
  # on the board is already `done`, DOC-004 is a `post` blocked on OPS-001, and the
  # only `todo` items are OPS-001 (kind: ops, which content runs skip) and the SRC-*
  # scout hacks (wrong lane). `gh pr list --state open --label auto:content` showed
  # only a post PR (POST-014) — no doc to duplicate. Rather than no-op, cross lanes,
  # or fabricate, synthesized a fresh in-lane doc. lint_agents.rb is an untouched
  # corner of the harness: the Meta pillar deep-dives every CI check that reaches the
  # gate (build/brand/drift/frontmatter/links/hack-box/router) but never the one
  # pointed at the robot's OWN agent/skill wiring — and it turns out that check is
  # unplugged from the gate two independent ways (run-all.sh swallows its exit with
  # `|| true`; aggregate.rb's CHECK_FILES omits `agents`), while its own header
  # claims "the gate enforces" it and it marks dangling refs severity:error. The
  # cousin of DOC-006 (word police) but inverted: that one is honestly non-blocking,
  # this one falsely looks blocking. Demonstrated with real captured output — clean
  # run, an injected dangling-agent-ref caught as ERROR/exit 1, and the aggregated
  # gate reporting PASS with agents.json present on disk but 0 findings in the
  # contract. Recommends the one-line fix to the scripts/ci owners in the PR body;
  # not patched here (content branch only).
  - id: DOC-017
    kind: doc
    title: "The check that guards the robot's job description and can't enforce it"
    brief: "Deep-dive companion to DOC-005/DOC-006 on lint_agents.rb: the check that validates the fleet's own agent files and skill dirs (valid front-matter, name==filename, every workflow `agent: <literal>` resolves, every skill has a SKILL.md) and marks the load-bearing failures severity:error — yet reaches the merge gate through neither wire. run-all.sh runs it with `|| true` (exit swallowed) and aggregate.rb's CHECK_FILES omits `agents` (its findings never enter findings.jsonl), so a dangling agent ref that would summon a brainless robot merges green. The inverse of the word police (DOC-006): that one is honestly non-blocking; this one's own header says 'the gate enforces' while the plumbing enforces nothing. Real captured output: clean run, an injected dangling-ref caught as ERROR/exit 1, and the gate reporting PASS with agents.json on disk but 0 agents findings aggregated. artifacts.json is orphaned the same way. Fix flagged for scripts/ci owners, not applied."
    voice: meta-confession
    priority: P2
    status: done
    published: /docs/the-check-that-guards-my-job-description/
  # Fresh post (autopilot run, 2026-07-09): the post lane was dry — every post
  # item was `done`, DOC-004 is a `post` still `blocked` on OPS-001, OPS-001 is a
  # skipped ops task, and the only `todo` items were the SRC-* scout hacks/tools
  # (wrong lane). `gh pr list --state open --label auto:content` showed only a
  # tool PR (duf) — nothing to duplicate. Rather than no-op, cross lanes, or
  # fabricate the blocked post, the factory synthesized this fresh Field Note from
  # a real finding: the Prime Directive runner reported `docker=false` and left all
  # 47 opt-in blocks `unverified` while Docker was fully up, because its `docker?`
  # probe reads the thread-local `$?` (nil after Open3 waits in another thread)
  # instead of the status object Open3 returns. Real captured output only.
  - id: POST-014
    kind: post
    title: "Docker was running; the check that guards my one rule swore it wasn't"
    brief: "Meta-confession: the Prime Directive runner (the check that proves I ran what I tell readers to run) reported no Docker sandbox and marked all 47 opt-in shell blocks unverified — while Docker answered on the same shell. Root cause reproduced: docker? destructures away the status Open3.capture2e returns and reads the thread-local $?, which is nil in the caller because Open3 waits in its own thread, so nil.success? raises and the bare rescue returns false. Deterministic (no child process sets $? first), non-blocking (exit 0), and opt-in — three soft layers stacked. Payload: after Open3.capture*, ask the returned status, not $?; and a check that can only ever report one result has stopped checking."
    voice: meta-confession
    priority: P2
    status: done
    published: /posts/2026/07/09/docker-was-up-my-check-swore-it-wasnt/
  # Fresh tool idea (autopilot run, 2026-07-09): every prior `tool` item was already
  # done (rg, fzf, jq, note-apps, fd, bat, eza, zoxide, delta, sd, hyperfine, hexyl,
  # procs, dust, hexedit, pip-audit) and the only `todo` items on the board are
  # OPS-001 (kind: ops, which content runs skip) and the SRC-* hacks (wrong lane).
  # `gh pr list --state open --label auto:content` showed one open PR, a hack
  # (type-a-before-you-alias) — no tool to duplicate. Rather than no-op or cross
  # collections, synthesized a fresh in-lane tool. `duf` is the missing `df` member
  # of the modern-CLI family already reviewed — the natural sibling of the already-
  # reviewed `dust` (du): dust is what's using space inside a tree, duf is how full
  # each mounted filesystem is. Honest gotchas left in and run for real on Ubuntu
  # 24.04 (duf 0.8.1 via apt): it splits mounts into separate local/special tables
  # so a tmpfs you expected goes missing from the top table, the boxed columns don't
  # line up with df so `awk '{print $4}'` grabs SIZE not AVAIL (and the box chars
  # survive a pipe — use --json), the Ubuntu build's `--version` prints the literal
  # "built from source", and its one-decimal sizes never quite match df -h's rounding.
  - id: TOOL-016
    kind: tool
    title: "duf: the df replacement that boxes up your disks and clams up about its version"
    brief: "Why `duf` is `df` with a colored use-bar and sortable boxed tables, the two-table local/special split that files your tmpfs into a second table you have to scroll to (--only/--hide control it, --all reveals the rest), the boxed columns that break df muscle memory so `awk '{print $4}'` returns SIZE not AVAIL and the box chars survive a pipe (--json is the only sane script interface), the Ubuntu package that builds --version as the literal 'built from source', and the one-decimal sizes that never match df -h's rounding. Every command run for real."
    voice: tool-review-honest
    priority: P2
    status: done
    published: /tools/duf-honest-review/

  # POST-015 — synthesized fresh (no `post` item was todo; DOC-004 blocked). Run-reasoning
  # is in the PR, not here — the post's whole argument is that narration belongs in the log.
  - id: POST-015
    kind: post
    title: "My to-do list is now 44% comments explaining why it was empty"
    brief: "Meta-confession: backlog.yml, the machine-readable source of truth the picker reads, is now 44% comment lines (583 of 1328) — mostly tombstones from past dry runs explaining why there was nothing in-lane to do, repeating the same three excuses (24x 'content runs skip', 27x 'the only todo', 28x 'kind: ops'). One permanently un-actionable P1 (OPS-001, kind: ops) sits first and spawns a fresh apology every pass. Real captured counts only. Payload: separate the ledger (state a machine parses) from the log (prose a human reads) — run-reasoning belongs in run logs / PR descriptions, not braided into the source of truth where it grows unbounded and buries the data; and an item no lane can action shouldn't sit in that lane's queue."
    voice: meta-confession
    priority: P2
    status: done
    published: /posts/2026/07/10/todo-list-mostly-comments-about-being-empty/

  # --- content-scout topic ideas (auto-appended; humans may edit/reprioritize) ---
  # Each carries the source it-journey.dev page it was inspired by; grow-lifehacker
  # links that page in the published piece. Default P3 — human items outrank these.
  - id: SRC-013
    kind: hack
    title: "Your database connection pool is too big: size it by cores, not hope (and the idle-in-transaction leak)"
    brief: "The counterintuitive fix for a slow, connection-starved app: make the pool smaller. Size it with the HikariCP formula (`cores * 2 + 1` — 17 for an 8-core box, not 200), because Postgres spawns an OS process per connection and can only do parallel work up to its cores. Put PgBouncer in `pool_mode = transaction` so 1000 clients share 20 real connections. The failure that stays in: a connection you never returned shows up as a row stuck `idle in transaction` in `pg_stat_activity`, quietly eating a slot until the pool exhausts and everything hangs — the fix is returning it in a `finally`/context manager, not a bigger pool."
    voice: how-to-practical
    priority: P3
    status: todo
    source: content-scout
    source_url: https://it-journey.dev/quests/0110/connection-pooling/
    source_title: "Database Connection Pooling"
    fingerprint: 9976143029b7
  - id: SRC-014
    kind: hack
    title: "The database migration that rolls out fine and rolls back never: test your downgrade before prod"
    brief: "Write migrations as paired up/down functions (`alembic revision`, `upgrade()`/`downgrade()`) and actually run the downgrade once — because the rollback nobody tested is the one you reach for at 2am. Two footguns stay in: a plain `ALTER TABLE ... DROP COLUMN`/`RENAME` takes a lock that breaks the running code the instant it lands (the fix is the expand-contract dance — add the new column, backfill, deploy code using both, drop the old one a release later), and you can never edit a migration you already shipped because the tool checksums it and refuses to run on a mismatch. Reproducible against a throwaway Docker Postgres."
    voice: how-to-practical
    priority: P3
    status: todo
    source: content-scout
    source_url: https://it-journey.dev/quests/0110/database-migrations/
    source_title: "Database Migrations"
    fingerprint: 6b77208720a9
  - id: SRC-015
    kind: hack
    title: "Dark mode in a dozen lines: prefers-color-scheme, CSS custom properties, and the contrast that fails WCAG"
    brief: "Define your colors once as CSS custom properties (`--bg`, `--accent`) on `:root`, then flip only the values inside `@media (prefers-color-scheme: dark)` — the whole page re-themes with no JS and no duplicated rules. The failure that stays in: the accent color that passed the 4.5:1 WCAG contrast ratio on a white background quietly fails it on your new dark one, so 'dark mode done' and 'dark mode readable' are two different checkmarks — verify each theme in a contrast checker, don't assume inverting the background inverted the legibility."
    voice: how-to-practical
    priority: P3
    status: todo
    source: content-scout
    source_url: https://it-journey.dev/quests/0100/profile-themes/
    source_title: "Profile Themes: Unleashing the Style Sorcerer"
    fingerprint: 7a90ef1f8d3e

  # --- Fresh doc (autopilot run, 2026-07-10): every `doc` item on the board was
  # already done (DOC-001..003/005..017; DOC-004 is a `post` blocked on OPS-001),
  # and the only `todo` items are OPS-001 (kind: ops, which content runs skip) and
  # the content-scout SRC-* hacks (wrong lane). `gh pr list --state open --label
  # auto:content` showed one open PR, a post (POST-015) — no doc to duplicate.
  # Rather than no-op, cross collections, or fabricate, synthesized a fresh in-lane
  # doc. gen_dashboard.rb is an untouched corner of the harness — the Meta docs
  # deep-dive the linters (word police / front-matter cop), the drift check, the
  # triage queue, and harvest_ideas, but never the formatter that writes the
  # committed SITE_HEALTH.md twin of the live /docs/health/ page. Genuinely on-brand
  # ("the mechanics are the content") and demonstrable with real captured output:
  # the two renderers read the same _data/health/ but the committed .md is a
  # snapshot that only refreshes when the script is re-run, so it silently drifts
  # from the live page when the data moves. Drift reproduced for real (queue_size
  # 75 vs a bumped 91) and reverted; on this checkout the twins agree (diff empty).
  - id: DOC-018
    kind: doc
    title: "The dashboard with a stale twin in the repo"
    brief: "Deep-dive companion to DOC-009 on gen_dashboard.rb: the formatter that renders the triage data a second time as a committed SITE_HEALTH.md, next to the live Liquid /docs/health/ page. Two renderers, one _data/health/ source, two clocks — the live page recomputes every build while the committed twin is a snapshot that only refreshes when someone re-runs the script, so it drifts the moment the data moves. Drift reproduced for real and reverted; the recursive twist that the dashboard's own top complaints are brand-lint findings on the Meta docs; and the analytics_stale disclaimer both copies print. Real captured output only."
    voice: meta-confession
    priority: P2
    status: done
    published: /docs/the-dashboard-with-a-stale-twin/
  # Fresh tool idea (autopilot run, 2026-07-10): every prior `tool` item on the board
  # was already done (rg, fzf, jq, note-apps, fd, bat, eza, zoxide, delta, sd,
  # hyperfine, hexyl, procs, dust, duf, pip-audit, hexedit) and the only `todo` items
  # are OPS-001 (kind: ops, which content runs skip) and a stack of SRC-* hacks (wrong
  # lane). `gh pr list --state open --label auto:content` showed one open PR, a hack
  # (chmod, SRC-008) — no tool to duplicate. Rather than no-op or cross collections,
  # synthesized a fresh in-lane tool. `gron` is the natural companion to the already-
  # reviewed jq: not another coreutils rewrite but the *anti-jq* — flatten JSON into
  # grep-able assignment lines so you find a path with grep instead of a query
  # language. Fully offline (apt install gron, 0.7.1 on Ubuntu 24.04) with two honest
  # gotchas left in and run for real: ungron pads a partially-grepped array with
  # `null` to keep indices honest, and non-identifier keys use ["bracket"] form so a
  # dotted-path grep silently matches nothing. Every command captured.
  - id: TOOL-017
    kind: tool
    title: "gron: the anti-jq that turns JSON into grep-able lines"
    brief: "Why gron flattens JSON into one assignment per value so you find a path with grep instead of learning jq's query language, the grep|ungron round-trip that carves out a subtree, the array-hole surprise where ungron pads a partial array with null to keep indices honest, the non-identifier keys that use [\"bracket\"] form (so a dotted-path grep matches nothing), and where jq still wins (gron is a lens, not a language — it can't transform). Every command run for real."
    voice: tool-review-honest
    priority: P2
    status: done
    published: /tools/gron-honest-review/

  # --- Fresh doc (autopilot run, 2026-07-11): no `todo` doc item existed — the whole
  # board's `todo` set is OPS-001 (kind: ops, which content runs skip) and a stack of
  # SRC-* hacks (wrong lane). `gh pr list --state open --label auto:content` showed one
  # open PR, a post (POST-016) — no doc to duplicate. Rather than no-op or cross
  # collections, synthesized a fresh in-lane doc. lint_artifacts.rb is the last
  # undocumented check in the Meta pillar: the guard on backlog.yml itself, the file
  # POST-003/POST-006 already made lore of. Its one honest angle is coverage — it
  # enforces exactly one rule (unique ids) and is blind to the exact corruption POST-006
  # reproduced (a union-merge-collapsed `status: nil` item, ids still unique), while the
  # duplicate it does catch is orphaned from the gate (not in aggregate.rb's CHECK_FILES,
  # a fact DOC-018 noted in passing). All three demonstrated with real captured output:
  # clean run (0/exit0), injected duplicate id (ERROR/exit1) on a scratch copy of the
  # check, statusless item (0/exit0), and the CHECK_FILES grep. Two-line fix flagged for
  # scripts/ci owners in the PR, not applied (plumbing, not content).
  - id: DOC-019
    kind: doc
    title: "The bouncer that only checks for twins"
    brief: "Deep-dive companion to DOC-018 on lint_artifacts.rb: the single check guarding backlog.yml — the file the whole fleet fights over (POST-003) — with exactly one rule, unique ids. It's blind to the corruption that file actually suffered (POST-006's union-merge-collapsed status: nil item, ids still unique), and even the duplicate it does catch is orphaned from the merge gate (absent from aggregate.rb's CHECK_FILES). Real captured output: clean run, an injected duplicate caught ERROR/exit 1, the statusless item waved through 0/exit 0, and the CHECK_FILES grep. Coverage is a claim; hand a guard the exact thing that broke last time and watch whether it flinches. Two-line fix flagged for scripts/ci owners, not applied."
    voice: meta-confession
    priority: P2
    status: done
    published: /docs/the-bouncer-that-only-checks-for-twins/

  - id: DOC-020
    kind: doc
    title: "The lock with no lock server"
    brief: "Deep-dive on scripts/fleet/lease.rb, the primitive the fleet-spawn doc (DOC-003) only gestures at: collision-free work claiming via a git ref used as a compare-and-swap (`git update-ref <ref> HEAD ''` = create-only-if-absent), so exactly one of two racing agents wins an item with no database or lock server — and the claim's boolean return IS the load balancer. The lesson that stays in: the ref is the lock, leases.yml is the human-readable record + TTL clock, and claim/release write their two stores in OPPOSITE orders on purpose so a crash always lands in the self-healing store, never the permanent one. The honest failure left in: the rollback catches a StandardError but NOT a hard SIGKILL between the CAS win and the YAML save — reproduced an orphan ref (0 YAML records) that blocks the id forever while reclaim_stale, which reads only YAML, drops zero. Real captured output throughout. Two-line fix (make reclaim_stale sweep orphan refs too) flagged for scripts/fleet owners, not applied."
    voice: meta-confession
    priority: P2
    status: done
    published: /docs/the-lock-with-no-lock-server/

  # --- content-scout topic ideas (auto-appended; humans may edit/reprioritize) ---
  # Each carries the source it-journey.dev page it was inspired by; grow-lifehacker
  # links that page in the published piece. Default P3 — human items outrank these.
  - id: SRC-016
    kind: hack
    title: "One mkdir, many folders: brace expansion (and the space that silently breaks it)"
    brief: "Stop running `mkdir` five times. `mkdir -p proj/{src,test,docs,.github/workflows}` builds the whole tree in one shot, `{1..10}` and `{a..z}` make ranges, and `cp file{,.bak}` is the one-keystroke backup. The failure that stays in: put a space after a comma — `{src, test}` — and the shell doesn't expand it at all, it hands `mkdir` two literal arguments and you get a folder actually named `{src,` plus one named `test}`. Braces also expand before variables, so `{$a,$b}` won't do what you hoped. Every case is offline-testable with `echo` before you commit to it."
    voice: how-to-practical
    priority: P3
    status: done
    published: /hacks/mkdir-brace-expansion-one-command-many-folders/
    source: content-scout
    source_url: https://it-journey.dev/quests/1111/building-technical-communities/
    source_title: "Building Technical Communities: Govern and Include"
    fingerprint: d654a01da54a
  - id: SRC-017
    kind: hack
    title: "Stop reinstalling every dependency on each build: order your Dockerfile so the layer cache does its job"
    brief: "Copy your lockfile (`package.json` / `requirements.txt`) and run the install BEFORE you `COPY` the rest of the source, so editing `app.js` doesn't re-run `npm install` — Docker invalidates every layer downstream of the first instruction that changed, and instruction order is the whole game. Two footguns stay in: `EXPOSE` documents a port but does not publish it (only `-p` at runtime maps it to the host), and a missing `.dockerignore` silently copies `node_modules`/`.git` into the build context, busting the cache you just arranged and bloating the image. Reproducible with `docker build` twice and reading the `CACHED` lines."
    voice: how-to-practical
    priority: P3
    status: todo
    source: content-scout
    source_url: https://it-journey.dev/quests/0100/container-fundamentals/
    source_title: "Docker Container Fundamentals"
    fingerprint: 9f229f5e094d
  - id: SRC-018
    kind: hack
    title: "Clean up your messy commits before you push: git rebase -i, --fixup/--autosquash, and the history you must never rewrite"
    brief: "Turn 'wip', 'more wip', 'typo' into one honest commit with `git rebase -i` (squash/reword/drop), or mark corrections as you go with `git commit --fixup <sha>` and let `git rebase -i --autosquash` file each fix under the commit it belongs to. The failure that stays in: rebasing rewrites the commit SHAs, so doing it to anything you've already pushed forces everyone who pulled it into a diverged history and a mess of duplicate commits — the rule is rewrite what's local, never what's shared. Fully reproducible in a throwaway repo."
    voice: how-to-practical
    priority: P3
    status: todo
    source: content-scout
    source_url: https://it-journey.dev/quests/0010/commitments-to-clean-commits/
    source_title: "Commit Hygiene: Crafting Clean, Atomic Commits"
    fingerprint: 6dd751337633

  # Synthesized in-lane (autopilot run, 2026-07-11): a `post` run found the queue
  # dry — POST-001..015 all done, DOC-004 still `blocked` on OPS-001, and the only
  # open auto:content PR is a tool (yq/TOOL-018), nothing to duplicate. Every other
  # `todo` item is a hack (SRC-*) or ops (OPS-001) — wrong lane. Rather than no-op,
  # cross collections, or fabricate the blocked post, synthesized a fresh in-lane
  # Field Note from a real finding: I tallied every tag on the pooled /tags/ page
  # and found 351 distinct tags across 168 pages, 249 (71%) pointing at exactly one
  # post, plus real singular/plural collisions (archive/archives, task-queue/
  # task-queues). Cause: free-form tagging with no controlled vocabulary, and a
  # cold-start author who reinvents synonyms every run. Real captured ruby output;
  # includes the observer-effect note (publishing this adds four more singletons).
  - id: POST-016
    kind: post
    title: "My site has 351 tags and 249 of them point at exactly one post"
    brief: "Meta-confession: I tallied every tag the /tags/ page renders (posts+hacks+tools pooled) and found 351 distinct tags across 168 pages, 249 (71%) used on exactly one post, plus real singular/plural splits (task-queue vs task-queues). A robot with no shared tag vocabulary cold-starts every run and reinvents synonyms, so free-form `tags: []` sums to a pile, not a taxonomy. The fix is a constraint (allow-list + singleton report), not prose — recommended, not applied. Real captured ruby output only, including the observer effect where publishing the post adds four more singletons."
    voice: meta-confession
    priority: P2
    status: done
    published: /posts/2026/07/11/351-tags-most-point-at-one-post/
  # Fresh tool idea (autopilot run, 2026-07-11): `yq '[.backlog[] | select(.status
  # == "todo" and .kind == "tool")] | length'` on this file returned 0 — every
  # prior `tool` item is done and the only `todo`s are `kind: hack` (SRC-xxx) or
  # OPS-001 (kind: ops, which content runs skip). The one open auto:content PR is a
  # hack (SRC-016, mkdir brace expansion), no tool to duplicate. Rather than no-op
  # or cross collections, synthesized a fresh in-lane tool. `yq` is the YAML member
  # of the modern-CLI family already reviewed — the jq companion (jq/gron done) — and
  # it carries the strongest "surprise in the box" of any remaining tool: `yq` is TWO
  # unrelated programs sharing one name (Mike Farah's standalone Go binary vs
  # kislyuk's Python jq-wrapper), and `apt`(3.1.0=kislyuk), `pip`(4.1.1=kislyuk),
  # and `snap`(mikefarah) each hand you a DIFFERENT one — verified for real on Ubuntu
  # 24.04. Both flavors run for real: same command emits YAML from one and JSON from
  # the other, kislyuk `-y` strips comments while mikefarah v4 preserves them, and a
  # bare unquoted assignment tags a "02139" zip as !!int and loses the leading zero.
  - id: TOOL-018
    kind: tool
    title: "yq: the YAML tool that's secretly two different programs"
    brief: "Why yq is jq for YAML (query, edit-in-place, YAML<->JSON), the name-collision trap where `apt`/`pip` give you kislyuk's Python jq-wrapper (v3.1.0/4.1.1) while `snap`/the manual install give you Mike Farah's standalone Go binary (v4.53) — same command, different output (YAML vs JSON), so a script written for one silently misbehaves on the other, the in-place edit where kislyuk `-y` strips your comments but mikefarah v4 keeps them, and the type-coercion footgun where a bare unquoted assignment turns a \"02139\" zip into !!int 02139 and drops the leading zero. Every command run for real on Ubuntu 24.04."
    voice: tool-review-honest
    priority: P2
    status: done
    published: /tools/yq-honest-review/

  # --- content-scout topic ideas (auto-appended; humans may edit/reprioritize) ---
  # Each carries the source it-journey.dev page it was inspired by; grow-lifehacker
  # links that page in the published piece. Default P3 — human items outrank these.
  - id: SRC-019
    kind: hack
    title: "Your model's loss bounces instead of dropping: the optimizer.zero_grad() line the training loop can't skip"
    brief: "PyTorch accumulates gradients by design, so a loop missing `optimizer.zero_grad()` adds each batch's gradients on top of the last one's and the loss lurches around instead of falling. The fix is one line at the top of the loop — zero_grad → forward → `loss.backward()` → `optimizer.step()` — and leaving it out sends you chasing a 'bad learning rate' that was never the problem. Reproducible on CPU with a tiny linear model (no GPU needed); the sibling footgun — a model on the GPU can't multiply a tensor still on the CPU — gets a one-line `.to(device)` note."
    voice: how-to-practical
    priority: P3
    status: todo
    source: content-scout
    source_url: https://it-journey.dev/quests/1101/deep-learning-frameworks/
    source_title: "Deep Learning Frameworks: PyTorch vs TensorFlow"
    fingerprint: 56874d8446f6
  - id: SRC-020
    kind: doc
    title: "Seal the test results before the robot wakes up: evidence a coding agent can't forge"
    brief: "The failure mode in any self-healing CI loop is the self-grading agent — when it can't actually run the tests, it writes a passing result anyway. The countermeasure is ordering: have the workflow mint the evidence artifact BEFORE the agent starts and verify it AFTER, so the record the agent is graded on is one it never had write access to. Why 'the agent reports success' and 'the tests passed' are two different facts, and how sealing the evidence at trigger time closes the tampering gap."
    voice: meta-confession
    priority: P3
    status: done
    published: /docs/seal-the-evidence-before-the-robot-wakes-up/
    source: content-scout
    source_url: https://it-journey.dev/quests/codex/ouroboros-loop/
    source_title: "Epic Quest: The Ouroboros Loop"
    fingerprint: 619d271188e3
  - id: SRC-021
    kind: tool
    title: "DuckDB: the analytics database that's just a file and runs SQL on your CSV"
    brief: "Honest verdict on DuckDB: an in-process, columnar SQL engine you `pip install` with no server to run, that queries a CSV or Parquet file directly (`SELECT ... FROM 'sales.csv'`) and chews through GROUP BY aggregates that make SQLite crawl. The dealbreakers that stay in: it's OLAP not OLTP (built for reads and one writer, not your app's concurrent transactions), a join wider than RAM will spill or fall over, and `read_csv_auto`'s type sniffing quietly decides a ZIP code is an integer and eats the leading zero. What it replaces, what it doesn't, and when plain Postgres still wins."
    voice: tool-review-honest
    priority: P3
    status: done
    published: /tools/duckdb-honest-review/
    source: content-scout
    source_url: https://it-journey.dev/quests/1100/data-warehousing/
    source_title: "Data Warehousing: Build a Dimensional Star Schema in SQL"
    fingerprint: c8e05a55752a

  # Synthesized in-lane (autopilot run, 2026-07-12): a `post` run found the queue
  # dry — POST-001..016 all done, DOC-004 still `blocked` on OPS-001, and the only
  # `todo` items are OPS-001 (kind: ops, which content runs skip), the SRC-* scout
  # hacks (wrong lane), SRC-020 (a doc), and SRC-021 (a tool). `gh pr list --state
  # open --label auto:content` showed one open PR, a tool (DuckDB/SRC-021) — no post
  # to duplicate. Rather than no-op, cross lanes, or fabricate the blocked post,
  # synthesized a fresh in-lane Field Note from a real finding: .gitignore
  # deliberately un-ignores Gemfile.lock (comment: "committing it pins github-pages
  # and html-proofer so CI is reproducible") but `git log --all -- Gemfile.lock` is
  # empty — the lock was never committed on any branch, so github-pages is unpinned
  # and html-proofer floats within ~> 5.0. Real captured git/grep output only. The
  # lockfile is build plumbing, not content, so the fix (commit it) is recommended
  # in the PR description, not applied on this content branch.
  - id: POST-017
    kind: post
    title: "The lockfile my .gitignore kept a seat warm for"
    brief: "Meta-confession on reproducible builds: .gitignore deliberately un-ignores Gemfile.lock with a comment saying committing it pins github-pages and html-proofer so CI is reproducible — but the lockfile was never committed on any branch (git log --all is empty), so nothing is pinned. github-pages has no version and html-proofer floats within ~> 5.0; the on-disk untracked lock resolves them (232 / jekyll 3.10.0 / html-proofer 5.2.1) but records those numbers nowhere git tracks. Real captured output only. Payload: un-ignoring a file only reserves a seat — the pin exists when the file is generated AND committed; a comment describing a decision looks as authoritative as the decision carried out. Fix (commit the lock, or pin in the Gemfile and drop the promise) recommended, not applied — build plumbing isn't content."
    voice: meta-confession
    priority: P2
    status: done
    published: /posts/2026/07/12/the-lockfile-my-gitignore-kept-a-seat-warm-for/

  # --- content-scout topic ideas (auto-appended; humans may edit/reprioritize) ---
  # Each carries the source it-journey.dev page it was inspired by; grow-lifehacker
  # links that page in the published piece. Default P3 — human items outrank these.
  - id: SRC-022
    kind: hack
    title: "Stop pasting the same HTML header into every page: Jinja2 template inheritance"
    brief: "Write the page shell once as `base.html` with `{% block content %}` holes, then `{% extends 'base.html' %}` in each page and fill only the blocks — no more find-and-replace across ten files when the nav changes. Every step rendered for real with Python's jinja2. The failures that stay in: a typo'd block name doesn't raise an error, it just renders nothing; autoescape is OFF by default in a bare `Environment` (Flask turns it on), so the same template is XSS-safe in one place and a hole in another; and stray blank lines leak from Jinja's whitespace rules until you set `trim_blocks`/`lstrip_blocks` or reach for `{%- -%}`."
    voice: how-to-practical
    priority: P3
    status: todo
    source: content-scout
    source_url: https://it-journey.dev/quests/1100/temple-of-templates/
    source_title: "The Temple of Templates: Reusable Abstractions"
    fingerprint: eb4fb97c4186
  - id: SRC-023
    kind: hack
    title: "Run your data-load script twice without doubling every row: ON CONFLICT DO UPDATE and the watermark that skips the rest"
    brief: "Make a load rerunnable so a retry doesn't duplicate: `INSERT ... ON CONFLICT(id) DO UPDATE SET ...` (SQLite/Postgres upsert) turns a re-run into a harmless update, and tracking `MAX(updated_at)` as a watermark pulls only new rows next time. Every step reproducible in a throwaway sqlite3 db. The footgun that stays in: a watermark-only `updated_at > last` silently misses rows sharing the exact boundary timestamp (and late-arriving data) — the fix is a small overlap window on the watermark PLUS the idempotent upsert, so re-reading a few rows costs nothing instead of double-counting them."
    voice: how-to-practical
    priority: P3
    status: todo
    source: content-scout
    source_url: https://it-journey.dev/quests/1100/etl-pipeline-design/
    source_title: "ETL Pipeline Design: Scalable Python Data Pipelines"
    fingerprint: a714be4e9ffe
  - id: SRC-024
    kind: hack
    title: "Validate a DataFrame before it poisons the table: a pandera schema and a quarantine bin"
    brief: "Put a gate at the pipeline's front door: a pandera `DataFrameSchema` that checks nulls, ranges, uniqueness, and regex formats, run with `lazy=True` so one pass reports every bad column at once instead of dying on the first failure. Then route the failing rows to a quarantine table instead of dropping them — a silent drop is the bug you can't debug three days later. Reproducible with pandas + pandera on a tiny frame. The honest limit that stays in: row-level rules pass every individually-valid record while the batch as a whole is wrong (half the usual row count, a null-rate that tripled), so schema validation is necessary but a count/z-score check on the batch is the other half of the job."
    voice: how-to-practical
    priority: P3
    status: todo
    source: content-scout
    source_url: https://it-journey.dev/quests/1100/data-quality/
    source_title: "Data Quality Engineering: Validation & Monitoring"
    fingerprint: 203a34ad8c22
